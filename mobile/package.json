{
  "name": "bookshelf-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Bookshelf Mobile — the iOS companion app for the BookForge Bookshelf library server",
  "scripts": {
    "build:web": "cd .. && npx ng build bookshelf-app --configuration mobile",
    "sync": "npm run build:web && npx cap sync ios && node scripts/register-native-plugin.mjs",
    "open": "npx cap open ios",
    "package:ios": "bash ./package-ios.sh"
  },
  "dependencies": {
    "@capacitor/core": "^8.4.1",
    "@capacitor/ios": "^8.4.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.1",
    "typescript": "^6.0.3"
  }
}
