node_modules/
dist/
out-tsc/
.angular/
release/

# The root .gitignore ignores every build/ for the engine's outputs; THIS one
# is electron-builder's buildResources — the app icon lives here and ships.
!build/

# a symlink, not a directory — the directory form above does not catch it (2026-09-19)
node_modules
