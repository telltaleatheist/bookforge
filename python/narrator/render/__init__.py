"""Render-side: session stores, workers, resume, progress.

In this phase only the layout-v1 reader (`session_v1`) and the FLAC STREAMINFO
reader (`flac_header`) exist; the worker layer arrives in migration step 3.
"""
