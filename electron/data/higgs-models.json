{
  "version": 2,
  "engine": "higgs",
  "_samplingRule": "ONE engine-level `sampling` for EVERY Higgs voice on BOTH arms, Listen streaming and book renders alike - Owen, 2026-09-06: \"temperature should be .8 everywhere, as it's the boson default\" (after \"we shouldnt deviate from the default unless we have a very good reason\"). It rides in every voice document (higgsVoiceCapsForModel -> higgsVoicesDocument -> narrator load_voices -> the arm's config override) and IS what renders. A per-backend `sampling` block may override it ONLY with a `_samplingNote` stating the REASON; a block without one is refused by higgsVoiceCapsForModel and by the keeper.",
  "sampling": {
    "temperature": 0.7,
    "topP": 0.95,
    "topK": 50
  },
  "_samplingNote": "REASON: 0.7 is MEASURED, and it replaces a default that was only ever asserted. Owen, 2026-09-09, after rendering known-gibberish chunks at both settings: \"gibberish gets cut in half at 0.7. set it to be global for higgs renders. streaming and rendering both.\" That supersedes the 2026-09-06 ruling recorded below, which chose 0.8 because it is Boson's own stated sampling rather than because anything here had been compared. SUPERSEDED NOTE (2026-09-06): Boson's own Higgs TTS 3 model card example is temperature 0.8 / top_k 50 (its only stated sampling); top_p 0.95 / top_k 50 are also what vllm-omni's deploy profile ships (v3_served.SERVER_DEFAULT_SAMPLING). The shipped generation_config.json inside each merged checkpoint says 1.0 and was NOT chosen: Owen picked 0.8 as \"the boson default\" (2026-09-06), with 1.0 / 0.7 / 0.8 put to him in plain words. Mac's 54-render WER table (deathstalker ckpt1080): 0.3 truncates, 0.6 and 1.0 tie; the pace bands were RE-MEASURED at 0.8 on 2026-09-07 (deathstalker 16.73, mistborn 15.01 chars/s; see each voice's pace.source); the training side's sweep found 0.6-0.9 flat on early-stop/run-on and babble beginning at 1.0, so 0.8 stays and 0.9 is the highest safe value. WHAT THE MOVE TO 0.7 DOES NOT INVALIDATE: it is inside the 0.6-0.9 band that sweep found flat, so the pace figures measured at 0.8 are not re-opened by it. If a pace band is ever re-measured, do it at 0.7.",
  "_comment": "Repo-tracked CURATED catalog for Higgs Audio v3 narration voices — the Higgs counterpart of orpheus-models.json, and deliberately NOT the same shape where the two engines differ. Orpheus's catalog OVERLAYS a per-machine runtime install manifest because Orpheus voices are folder-discovered. Higgs has no folder discovery: a Higgs voice is a row in THIS FILE, so there is no runtime manifest and no reconcile fallback — a voice that is not listed here does not exist, and higgs-models.ts refuses it BY NAME rather than falling through to the served model's own speaker (which measures at 12% of the narrator's ECAPA ceiling: a DIFFERENT person, not a weak clone). Every number in a backends block is MEASURED — provenance in E:\\training\\_campaigns\\2026-09-01-cod-full-rebuild\\higgs\\HIGGS_V3_LEVERS.md — or is an explicitly-marked placeholder that the loader REFUSES to render with. AND THERE IS ONE BLOCK PER BACKEND, not one per voice: `served` is vllm-omni behind WSL, `mlx` is the in-process mlx-audio sampler on the Mac, and the loader picks by ARM (wsl -> served, darwin -> mlx). They do not share numbers, because a cap is produced by RENDERING and the two arms sample through different implementations of top-k/top-p over different runtimes -- the same three sampling values make the configuration identical, not the draws (PORT_NOTES 13.11). So copying a merged directory to the other machine copies the WEIGHTS and not the CERTIFICATE; see _checkpointShapeComment and the deathstalker entry, whose mlx cap is null while its served cap is being measured.",
  "_wireFormatComment": "THIS FILE IS NOT THE WIRE FORMAT. narrator reads a VOICE DOCUMENT whose path arrives in NARRATOR_HIGGS_VOICES (see python/narrator/engine/higgs/config.py load_voices): `{ \"<voiceId>\": { \"clips\": [{path, transcript, seconds}], \"scene\"?, \"adapterDir\"?, \"allowedControls\"?, \"maxReferenceSeconds\"? } }`. In THAT format a Higgs voice is ALWAYS clips, and a fine-tune is an `checkpointDir` riding on the same object — there is no separate checkpoint kind. This catalog keeps an explicit `kind` anyway, because `kind` is what selects BOOKFORGE'S OWN RULES (above all: maxChars is REQUIRED and measured for an adapter, and may be the engine placeholder for a zero-shot voice) and because a picker has to tell a person which of the two they are choosing. `higgsVoicesDocument()` in higgs-models.ts translates one into the other; it is the only place the two shapes meet.",
  "_licenseComment": "Higgs Audio v3 (bosonai/higgs-audio-v3-tts-4b) ships under the Boson Higgs TTS 3 RESEARCH AND NON-COMMERCIAL license, and a fine-tune of those weights INHERITS it. That is why every entry carries an explicit `license` and `commercialUse: false`: this engine is enabled for Owen's personal evaluation and cannot ship in a commercial BookForge build without a separate licence from Boson AI. Higgs Audio v2 is the trainable-and-shippable family (Community licence: attribution plus 'Higgs Audio 2' in any fine-tune's name) and is NOT in this catalog — Owen dropped v2 on 2026-09-04 ('basically just Orpheus and we know Orpheus better').",
  "_checkpointFilesComment": "THE REQUIRED FILES OF A kind:checkpoint VOICE LIVE IN ITS checkpointDir, not in this catalog, and one of them is easy to lose: generation_config.json. vllm-omni serve resolves sampling FROM THE MODEL DIRECTORY (--generation-config defaults to auto), so that file IS the temperature/top_p/top_k every request is answered at; a merged dir without it falls through to the bare SamplingParams() in vllm_omni entrypoints/openai/stage_params.py — top_p 1.0, top_k DISABLED — which samples the untruncated 1026-way codebook tail and derails prompts over ~600 chars into babble (measured 2026-09-05). OpenAICreateSpeechRequest carries no sampling fields, so nothing per-request can correct it. PROVENANCE: bosonai/higgs-audio-v3-tts-4b ships NO generation_config.json (verified across the whole WSL HF cache and the Mac base snapshot) — the MERGE writes one into every merged dir from a recorded runs/<run>/generation_config.override.json whose values are vllm-omni own deploy/higgs_multimodal_qwen3.yaml stage-0 default_sampling_params ({temperature 1.0, top_p 0.95, top_k 50, repetition_penalty 1.0}), recording the source in merge_manifest.json beside the weights. So a checkpointDir must carry: the weights, config.json, tokenizer.json, tokenizer_config.json, chat_template.jinja AND generation_config.json. BookForge does NOT check this — the directory is inside WSL (or on the Mac) and this process cannot see it; narrator refuses BY NAME at load (v3_served.require_generation_config, reached through checkpoint_serve_target) on absence, unparseable JSON, or a file carrying none of temperature/top_p/top_k. On the Mac that file is narrator OWN sampling too: mlx-audio 0.4.8 does not read it and its Model.generate defaults top_p/top_k to None. See docs/HIGGS_ENGINE.md and python/narrator/engine/PORT_NOTES.md 12.8d + 13.11.",
  "_referenceComment": "EXACTLY ONE REFERENCE CLIP PER VOICE, 30 s of audio at the very most. vllm-omni refuses multi-shot cloning, so 'two clips' means ONE pre-joined wav (clips joined by 0.35 s of silence) whose transcript is the transcripts joined in the same order — the joining happens when the voice is STAGED, never at render time, and this catalog stores the joined result. Every clip must declare its `seconds`: narrator's v3_served.reference_seconds refuses a clip without one rather than probing the file, so a missing duration is a render that dies after the server has already spent five minutes coming up. Measured: no reference 0.093 speaker cosine, one clip 0.680, two joined 0.692, against a 0.766 narrator self-ceiling — and same-book-vs-cross-book is worth +0.076 where the second clip is worth only +0.012, so a reference set must come from ONE book.",
  "serving": {
    "_comment": "The shared v3 serving stack, selected by a model's engineVersion. A model entry may declare its own `serving` block, used INSTEAD of this one (never field-merged) so a voice has exactly one serving answer. narrator INVOKES the operator's launch script rather than writing its own — the CUDA_HOME/FlashInfer workarounds live there and a second copy would drift — so `launchScript` is what NARRATOR_HIGGS3_SERVE_SCRIPT points at.",
    "engineVersion": "v3",
    "stack": "sglang-omni",
    "_stackNote": "WHICH SERVING STACK. REQUIRED, NO DEFAULT (higgsServingStack refuses a block without it, and narrator refuses a spawn whose HIGGS_STACK is unset). FLIPPED TO 'sglang-omni' ON 2026-09-06, on Owen's ruling, on the measurements below. IT IS ONE WORD: the env, the launcher, the port, the memory fraction, the batch width, the request shape, the frame cap, the installer Settings runs and the rows the doctor reports all follow from it, and TO FLIP BACK it is the same one word ('vllm-omni'). Nothing else in this file changes either way - the vllm-omni knobs stay beside the sglang block precisely so neither stack's measured configuration is lost while the other is selected. THE KEEPERS TEST BOTH STACKS ALWAYS, against explicit fixture catalogs, and separately assert that whichever stack is SHIPPED here gets the matching variable set; so a flip moves which row is the shipped one and never which rows exist. WHY THE SECOND STACK EXISTS, measured 2026-09-05 by the training side on the same 50 packed chunks of Working Towards the Fuhrer, the same merged checkpoint (ckpt-1080), the same sampling and one seed, scored by whisper coverage / skipped words / insertion rate / early stop plus 3 s ECAPA windows for mid-chunk voice switches (orpheus-finetune/HIGGS_FIELD_NOTES.md 4n): vllm-omni 0.28.0 at 1 in flight = 0 early stops, 5/50 damaged, 0 sustained voice switches, 1,064 chars/min; vllm-omni at 16 = 4 early stops, 13/50 damaged, 6 sustained switches, 10,752 chars/min; SGLang-Omni 0.1.4 at 16 = 0 early stops, 5/50 damaged, 0 switches, 26,666 chars/min. vllm-omni's damage is its BATCHED TALKER and not the model or the text: the same build at width 1 is clean, and the corruption lands on the NEWEST batch row (the request that prefills while others decode, which vLLM's condense moves when an earlier row retires). The truncations Owen heard, the gibberish, and the sustained voice switches are all that one defect. What stays damaged on every clean engine (~10 %) is text-domain - em-dashes missing from the corpus, a footnote welded onto a chunk head, umlauts - and is fixed elsewhere. THE TWO STACKS ARE NOT INTERCHANGEABLE, which is why this key has no default: vllm-omni takes sampling inside extra_params and reads the checkpoint's own generation_config.json, while SGLang-Omni takes it at the request top level and reads NO such file (a request that sends nothing gets top_k disabled and the untruncated 1026-way codebook tail - measured: one chunk ran to the cap with 80 s of silence); the frame-cap field is max_new_tokens rather than max_tokens; the context is a hard-coded 4096 rather than 8192; the two site-packages patches apply to vllm-omni ONLY; and a reference-clone voice cannot render on SGLang at all.",
    "narratorEngineId": "higgs-v3",
    "model": "bosonai/higgs-audio-v3-tts-4b",
    "env": "higgs3",
    "condaEnvName": "higgs3",
    "launchScript": "serve_higgs_v3.sh",
    "servedModelName": "higgs-v3",
    "host": "127.0.0.1",
    "port": 8095,
    "endpoint": "/v1/audio/speech",
    "healthEndpoint": "/health",
    "gpuMemoryUtilization": 0.35,
    "codecGpuMemoryUtilization": 0.1,
    "maxModelLen": 8192,
    "maxNumSeqs": 16,
    "deployConfig": "higgs_default_frames7500.yaml",
    "_deployConfigNote": "A vllm-omni deploy profile, reaching the launcher as HIGGS_DEPLOY_CONFIG. It is a FILE NAME or a full path, NEVER a bare profile name: measured 2026-09-05, vllm-omni 0.28.0 resolves `--deploy-config <bare name>` to \"Deploy config not found\" (config_factory._load_user_deploy_config joins a bare name to the deploy dir without appending .yaml), and both the launcher and higgsSpawnEnv refuse a bare name before the launch rather than paying a 297 s cold start to read the crash. IT IS NO LONGER null. THE VALUE HERE IS A BARE FILE NAME AND BookForge RESOLVES IT: higgsSpawnEnv emits HIGGS_DEPLOY_CONFIG=<wsl conda base>/envs/<higgs env>/bin/higgs_default_frames7500.yaml — the SAME derivation that produces NARRATOR_HIGGS3_SERVE_SCRIPT, so the profile that runs is the installer's copy sitting beside the launcher and not whatever a relative name would resolve against in vllm-omni's own deploy directory. A value written here WITH a path separator is passed through verbatim instead. WHY A PROFILE IS SET AT ALL: see _frameCeilingNote — the auto-discovered profile caps every render at 2048 frames (81.92 s) and no request parameter can raise it. This is NOT the CUDA-graph question: higgs_multimodal_qwen3_low_latency.yaml (enforce_eager false, cudagraph_mode FULL_DECODE_ONLY) is a separate profile and still uncertified.",
    "_frameCeilingNote": "THE SERVED SPEECH ENDPOINT IGNORES A PER-REQUEST max_tokens, so stage 0's `default_sampling_params.max_tokens` in the deploy profile is a HARD CEILING on the audio length of EVERY render. MEASURED 2026-09-05 (owens-pc, RTX 3090 Ti, vllm-omni 0.28.0): vllm-omni's auto-discovered higgs_multimodal_qwen3.yaml sets it to 2048 frames = 81.92 s, and a chunk needing more audio than that is CUT MID-SENTENCE — silently, with the request reporting success. Raising it per request does nothing; the profile is the only lever. electron/scripts/higgs/higgs_default_frames7500.yaml is byte-identical to that auto profile EXCEPT max_tokens 7500 (300 s), which is what this block's deployConfig names. THE FILE'S BYTES ARE THE BINDING: the training side's certificates were measured against sha256 24d288f193eaa8c5…, so that file is edited by re-certifying and never in place — the installer copies it into <env>/bin/ on every run and the doctor's `profile-sha` row compares the deployed copy against the shipped one. It is a FILE rather than a write into site-packages because a pip upgrade in the env reverts site-packages (that is what reverts the two patches above) and cannot revert a profile passed on the command line.",
    "attentionBackend": "FLASH_ATTN",
    "sglang": {
      "_comment": "THE SGLang-Omni HALF, read when `stack` is \"sglang-omni\" and ignored otherwise. It sits BESIDE the vllm-omni knobs rather than replacing them so that flipping `stack` is a one-word edit and neither stack's measured configuration is lost. Every field reaches electron/scripts/higgs/serve_higgs_sgl.sh as a HIGGS_SGL_* variable, except maxRunningRequests, which travels as HIGGS_MAX_NUM_SEQS because narrator's serve_concurrency() reads exactly that name on both stacks and there must be ONE answer per job to 'how wide is this render'.",
      "condaEnvName": "sglomni",
      "_condaEnvNameNote": "A SEPARATE conda env from higgs3, and it must be: python 3.12 + torch 2.13.0+cu130 + sglang 0.5.18 + sglang-omni 0.1.4 + flashinfer 0.6.17 cannot share an environment with python 3.11 + vllm 0.28.0. It is NOT the wslHiggsCondaEnv setting (whose default is literally 'higgs3'); on this stack the env name comes from here, which is the only place the (env, launcher) pair is written down. Build it from Settings -> Higgs, which runs installScript. The env also needs CUDA_HOME pointed at <env>/lib/python3.12/site-packages/nvidia/cu13 with two symlinks inside it, `lib64 -> lib` and `libcudart.so -> libcudart.so.13`, without which flashinfer's nvcc JIT cannot build a kernel; install_sglomni.sh creates them and serve_higgs_sgl.sh points at them.",
      "launchScript": "serve_higgs_sgl.sh",
      "installScript": "install_sglomni.sh",
      "servedModelName": "higgs-v3-ds",
      "_servedModelNameNote": "--model-name on the launch line, the `model` field of every request, and the id /v1/models reports. DELIBERATELY NOT vllm-omni's 'higgs-v3': the two stacks answer the same shaped endpoints, so a name that differs is one more way a leftover server on the wrong port is caught before a book is rendered against it. NOTE THAT /v1/models CANNOT SAY WHICH CHECKPOINT IS UP on this stack: sglang_omni/serve/openai_api.py's _register_models answers ModelCard(id=model_name, root=model_name) - the served NAME in both fields, never the model path. narrator therefore reads HIGGS_MODEL_DIR back out of /proc/<listener pid>/environ, which is what its own launch wrapper exported into that process.",
      "host": "127.0.0.1",
      "port": 8200,
      "_portNote": "8200, so a server on this stack can never be confused with vllm-omni's 8095 - the same port the training side's probes used.",
      "memFractionStatic": 0.6,
      "_memFractionStaticNote": "--mem-fraction-static. ONE FRACTION FOR THE WHOLE ENGINE, unlike vllm-omni, which is two vLLM stages that each apply the global flag (hence gpuMemoryUtilization + codecGpuMemoryUtilization above and the --stage-overrides split in serve_higgs_v3.sh). MEASURED 2026-09-05 on owens-pc (RTX 3090 Ti, 24.5 GB): 0.60 holds ~19 GB at 16 in flight, healthy in ~110 s, with CUDA graphs captured on sm_86 (prefill + decode + 150 codec graphs).",
      "maxRunningRequests": 16,
      "_maxRunningRequestsNote": "--tts_engine.factory.max_running_requests, AND the width of narrator's own batch. It travels as HIGGS_MAX_NUM_SEQS (the same variable the vllm-omni arm uses) because serve_concurrency() reads that name and refuses when it is unset. 16 is the width every night-3 measurement was taken at: 0 early stops, 5/50 damaged, 0 sustained voice switches, 26,666 chars/min.",
      "cudaGraphMaxBs": 16,
      "_cudaGraphMaxBsNote": "--tts_engine.factory.cuda_graph_max_bs. A CAPTURE budget rather than a scheduling limit - graphs are captured up to this size at startup and cost VRAM - which is why it is its own field even though it ships equal to maxRunningRequests, which is how it was measured.",
      "maxNewTokens": 7500,
      "_maxNewTokensNote": "--tts_engine.factory.max_new_tokens, applied by make_higgs_scheduler_adapters as min(request, this). NOT the effective per-request cap: the real ceiling is contextTokens below, and 7500 is above it, so this flag is inert in practice on a 4096-token window. It is passed anyway because it is what the measured configuration passed.",
      "contextTokens": 4096,
      "_contextTokensNote": "THE HARD CONTEXT, and the one number that most constrains a voice's targetChars on this stack. sglang_omni/models/higgs_tts/engine_builder.py sets `class HiggsTtsEngineBuilder: context_length = 4096` as a class attribute - there is no CLI flag and no config path, and it cannot be raised from the launcher or from a request. Prompt tokens + max_new_tokens over 4095 is an HTTP 500 from inside the scheduler, not a shorter render. narrator sizes every request against it and REFUSES BY NAME when a chunk cannot fit (python/narrator/engine/higgs/sgl_served.py frame_cap, which sends min(cap_frames, headroom) and raises when the headroom drops under 1.2x the chunk's expected length). At the measured book pace (5.82 s per 100 characters) that boundary is around 1,900 characters, above every certified Higgs targetChars. It is ALSO why a reference-clone voice is refused on this stack: a clip's ~330 placeholder tokens come out of the same window and measurably pushed the 13 longest chunks of a real book over it at a 3,500-frame cap (HIGGS_FIELD_NOTES 4n.10).",
      "coldStartSeconds": 110,
      "_coldStartSecondsNote": "Launch to /health answering, MEASURED on owens-pc 2026-09-05 (night3/logs/serve_events.log). Well inside narrator's 900 s READY_TIMEOUT_SECONDS, which raises immediately if the process actually died - so the patience is only ever paid while something is genuinely coming up.",
      "patches": [],
      "_patchesNote": "EMPTY, AND THAT IS A FACT RATHER THAN A GAP. The two site-packages patches above (patch_vllm.py and patch_sentinel_filter.py) are fixes to vllm and vllm_omni. SGLang-Omni has its own stage processor, is not patched, and has nothing of that kind to prove - which is why BookForge's doctor reports no patch rows for this stack and narrator runs no sentinel-filter proof on it."
    },
    "coldStartSeconds": 297,
    "readyTimeoutSeconds": 300,
    "_servingNote": "EVERY FIELD IN THIS BLOCK NOW REACHES THE LAUNCH SCRIPT, and until 2026-09-05 none of them did. electron/higgs-models.ts:higgsSpawnEnv emitted only the NARRATOR_* set, so electron/scripts/higgs/serve_higgs_v3.sh ran on its own built-in defaults and this block was a description of a configuration nothing applied — editing maxNumSeqs here changed nothing at all, which is worse than having no field, because it is a lever that reports success. The route is now: higgsServingFor(model) -> higgsSpawnEnv -> HIGGS_* environment -> buildNarratorSpawn's `export K=V` prefix into the guest -> the worker's env -> serve_higgs_v3.sh. The mapping, one per line: host -> HIGGS_HOST, port -> HIGGS_PORT, gpuMemoryUtilization -> HIGGS_GPU_MEM_UTIL, codecGpuMemoryUtilization -> HIGGS_CODEC_GPU_MEM_UTIL, maxModelLen -> HIGGS_MAX_MODEL_LEN, maxNumSeqs -> HIGGS_MAX_NUM_SEQS, deployConfig -> HIGGS_DEPLOY_CONFIG (emitted only when it is not null, and RESOLVED TO AN ABSOLUTE GUEST PATH when it is a bare file name — see _deployConfigNote), plus HIGGS_ENV, which is the conda prefix the spawn derives and the script builds CUDA_HOME/PATH/LD_LIBRARY_PATH from. TWO OF THEM ARE NOT ONLY THE SCRIPT'S: narrator re-exports HIGGS_HOST/HIGGS_PORT/HIGGS_MAX_NUM_SEQS into the wrapper it launches (v3_served.py _launch_exports), so the pair that binds and the pair narrator polls cannot drift, and serve_concurrency() sizes narrator's own batch from HIGGS_MAX_NUM_SEQS and REFUSES BY NAME when it is unset — which is why BookForge sets that one on every arm and every phase, not just where a server is started. HIGGS_MODEL_DIR is the exception in the other direction: narrator exports it per voice from the voice document's checkpointDir, so BookForge must never set it. A missing or out-of-range number here is REFUSED by name in higgsSpawnEnv rather than defaulted — these land on a vllm-omni command line five minutes before anything can be heard.",
    "_maxNumSeqsNote": "16 is vllm-omni's OWN profile value for stage 0 (higgs_multimodal_qwen3.yaml), and it replaced a 2 that was never anything but a placeholder — it reached nothing until 2026-09-05, so no measurement was ever taken against it. IT IS NOW ALSO A MEASURED CEILING AT THE SHIPPED MEMORY FRACTIONS: on owens-pc (RTX 3090 Ti, 2026-09-05) 16 concurrent at 0.35 + 0.10 ran 11,387-11,584 chars/min over three runs while 32 concurrent filled the card and stalled. THE CERTIFYING SERVER FOR deathstalker RAN AT 64 (see that voice's served _maxCharsNote) — at the OLD fractions and before that stall was measured — so 16 is not the width its cap was certified at. Nothing measured says whether batch width moves the safe chunk length; if it does, the deathstalker certificate is bound to 64 and this is the field that would have to change. It is also the width of narrator's own batch (serve_concurrency), so raising it raises concurrent POSTs and VRAM pressure together.",
    "_coldStartNote": "297 s from launch to /health answering, MEASURED on narrator's own GPU smoke (2026-09-04) — not the ~55 s the first audition reported, which was a warm cache. narrator's READY_TIMEOUT_SECONDS is 300, i.e. the measurement sits three seconds under the limit. Anything in BookForge that decides a Higgs job is dead must therefore allow AT LEAST five minutes before the first audio. Checked against the two that could: WORKER_STARTUP_TIMEOUT_MS is 10 minutes and WORKER_PROGRESS_TIMEOUT_MS is 12 (parallel-tts-bridge.ts), so both already clear it — but they clear it by minutes, not by an order of magnitude, and anyone tightening them must re-read this line. THE SERVER PREALLOCATES AND THEN OWNS THE CARD, in TWO STAGES, each fraction passed PER STAGE through --stage-overrides (vllm-omni applies a GLOBAL --gpu-memory-utilization to EVERY stage — omni_config.py _stage_cli_overrides — so the campaign's single `--gpu-memory-utilization 0.60` reserved 0.60 TWICE, measured holding 24.2 GB of a 24.5 GB card; the same flag was also clamping the codec stage's context length to maxModelLen 8192 against its profile's 65536). AND THE TALKER'S FRACTION IS ITS KV CACHE BUDGET ON TOP OF THE WEIGHTS, NOT A CAP ON THE STAGE — the server's own log at 0.35 (owens-pc, 2026-09-05): \"Desired GPU memory utilization is (0.35, 8.4 GiB). Actual usage is 7.72 GiB\", \"Available KV cache memory: 8.4 GiB\", \"GPU KV cache size: 61,120 tokens\". Reading it as a ceiling is what overcommitted the card. MEASURED, same box, same night: 0.60 + 0.25 held 24,274 MiB with the WDDM driver paging into shared system RAM and the render falling from ~8 to ~2 chunks/min; 0.55 + 0.15 held 24.0 GB and still paged; 0.35 + 0.10 holds 18.7-19.2 GB with IDENTICAL THROUGHPUT at 16 concurrent — 11,387-11,584 chars/min across three runs — which is why those are the shipped values. 32 concurrent at 0.35 + 0.10 filled the card and stalled.",
    "_flashInferNote": "FlashInfer is unavailable on sm_86 with torch 2.13+cu130 — it JIT-builds its attention kernels with nvcc and its bundled CCCL headers reject the CUDA 13 nvcc from the pip wheel — so the launch pins VLLM_USE_FLASHINFER_SAMPLER=0, VLLM_DISABLE_FLASHINFER_PREFILL=1 and --attention-backend FLASH_ATTN. IT IS ALSO STATED PER STAGE, in the stage-0 --stage-overrides object, and that is not redundant: measured 2026-09-05 (training), when a deploy profile is passed EXPLICITLY its stage-level attention_backend can beat the global flag — and this block now names a profile, so the global flag alone would no longer be enough to keep FlashInfer out. Speed only; the flashinfer-jit-cache cu130/sm_86 wheel from https://flashinfer.ai/whl/cu130/torch2.13/ is the route if v3 speed ever matters.",
    "patches": [
      {
        "id": "vllm-negative-token-id",
        "script": "patch_vllm.py",
        "target": "vllm/v1/engine/input_processor.py",
        "marker": "min_input_id != -100",
        "why": "vLLM 0.28's blanket negative-token-id rejection fires on vllm-omni's AUDIO_PLACEHOLDER_ID (-100), which the clone path deliberately emits for the talker to substitute reference embeddings at prefill. Without the patch EVERY clone request is HTTP 400 'Token id -100 is out of vocabulary' — which is what made cloning look broken in the first audition."
      },
      {
        "id": "higgs-sentinel-filter",
        "script": "patch_sentinel_filter.py",
        "target": "vllm_omni/model_executor/stage_input_processors/higgs_audio_v3.py",
        "marker": "_filter_sentinel_frames",
        "absentMarker": "[:, :-1]",
        "why": "Upstream substitutes every out-of-range code with 0 and then trims exactly ONE frame. Both halves are wrong: 0 is a VALID codec code, so the substitution CONVERTS the ramp-down BOC/EOC sentinels into real sound rather than neutralising them; and the sentinels smear across the last Q-1 = 7 frames (codebook c is delayed by c positions), so one frame leaves ~240 ms of audible garbage at the end of every chunk — Owen heard it as 'a stray syllable or sound after each sentence'. The patch decides by TOKEN IDENTITY instead: a frame is kept iff all 8 codebooks are in [0, 1023], nothing out of range ever reaches the codec, and there is no substitution and no positional trim left. The sync path gets the full filter; the streaming path gets the trailing run only, because Stage 1 trims left_context/right_holdback BY FRAME COUNT and dropping a leading or interior frame there would desync those trims and cut real speech. IT IS SERVER-SIDE: a patched server returns an already-clean tail and THE CLIENT MUST NOT TRIM AGAIN — what is left is a hard sample boundary, which is the assembler's 10/25 ms fade job. SUPERSEDES patch_tail_trim.py (retired 2026-09-05), which walked backwards from the end while frames were bad — positional reasoning that kept the 0-substitution for every sentinel outside the trailing run.",
        "_markerNote": "The marker is `_filter_sentinel_frames` and NOT `_trim_trailing_sentinel_frames`: this patch writes BOTH helpers (the trailing-run one is what the streaming path calls), and the retired patch_tail_trim.py wrote the trailing-run one too — so grepping for that would certify a band-aided file as patched. `absentMarker` is the other half of the proof and is asserted to be ABSENT: `[:, :-1]` is upstream's one-frame trim, which occurs twice in the pristine file and zero times after this patch (measured on the certifying box, vllm-omni 0.28.0, 2026-09-05). Present marker + absent trim is exactly 'the token-identity filter is in and no trim code remains', which is the half of the patch's proof that can be checked statically.",
        "_certifiedShaNote": "The stage processor this patch produces, on vllm-omni 0.28.0, is sha256 0b36f6507dd11653253bbebb278c3657e5d17a2a52f78018cd0bddd45a7ac210 (pristine: 376ca5647773cb191634b266b03bfefe490c080ef9f75aed045f1f31c9a19fb4), and the repo's electron/scripts/higgs/patch_sentinel_filter.py reproduces it byte for byte from the pristine file — verified 2026-09-05. THAT SHA IS PART OF A CERTIFICATE: a certificate binds (checkpoint dir, stage-processor sha, max_chars) together, so a change to this patch is a new server build that re-certifies rather than inheriting a published cap. See docs/HIGGS_ENGINE.md."
      }
    ],
    "_voiceSwitchNote": "A VOICE SWITCH IS A SERVER RESTART, and that is not a tuning choice — it is the only thing vllm-omni supports. It cannot load a LoRA at runtime: there are no adapter flags on `vllm-omni serve`, and the higgs_audio_v3 talker class does not implement SupportsLoRA. So every Higgs voice ships as a MERGED CHECKPOINT DIRECTORY (~8.5 GB) and the server is started ON that directory. Switching voice costs a restart: ~55 s warm (page cache hot) to ~300 s cold, against the 300 s readyTimeoutSeconds. A book renders in ONE voice, so this is paid once per job and never per chunk — but anything that imagined mixing voices within a render, or a cheap per-request voice cast, cannot be built on this stack."
  },
  "models": [
    {
      "id": "default",
      "label": "Higgs v3 built-in voice (no reference)",
      "kind": "default",
      "engineVersion": "v3",
      "voice": {},
      "license": "boson-higgs-tts-3-research-noncommercial",
      "commercialUse": false,
      "sampleRate": 24000,
      "addedAt": "2026-09-04",
      "backends": {
        "served": {
          "maxChars": 600,
          "maxCharsSource": "placeholder",
          "_maxCharsNote": "600 is the ZERO-SHOT figure and it is measured, not guessed: coverage by faster-whisper/difflib alignment is 1.000 at 213/440/504 chars and holds through 600, while 898 chars fails REPRODUCIBLY (coverage 0.86 and 0.78 on two seeds; tail coverage 0.314 and 0.000 — it drops the end of the paragraph). A reference clip does NOT fix it; it is a v3 model property that v2 does not have. DURATION RATIO IS NOT A COVERAGE PROXY: at temperature 0.7 the 898-char chunk produced ratio 0.99 while actually delivering coverage 0.778 with a 26% insert rate — it dropped text and padded the duration with hallucinated words, and any cap built on duration alone would have passed it. THIS NUMBER IS VALID ONLY FOR ZERO-SHOT and must never be copied onto an adapter voice; see the deathstalker entry.",
          "edgeFadeMs": {
            "in": 10,
            "out": 25
          },
          "_edgeFadeNote": "WHO APPLIES THIS: narrator's assembler, and as of narrator 4854aae4 it DOES — assemble/engine_profiles.py declares higgs-v3 as pads=false with 10/25 ms fades, assemble/edges.py applies them as RAISED-COSINE windows (zero slope at both ends, so the join has no discontinuity in the first derivative either), and _plan_unpadded realizes the manifest gapBefore/gapAfter as generated silence through one FLAC writer so the concat stays homogeneous. Prep writes the gaps.json sidecar these read, and session_v1 REFUSES a pads=false session without it. An earlier draft of parallel-tts-bridge.ts justified routing Higgs assembly to narrator by claiming narrator already read this field when neither side did; it does now, and the number is here because the catalog is where per-voice tuning lives. WHY THE FADE IS NEEDED AT ALL: Higgs emits NO lead/trail silence of its own — unlike Orpheus, which bakes pads into each chunk FLAC — and the patched server already trims its own sentinel tail, so what reaches the assembler ends at a hard sample boundary: a step discontinuity, i.e. a click. 25 ms out / 10 ms in removes it, taking a chunk edge from about -30 dB to -45..-48 dB. Applied at ASSEMBLY and never baked into the per-chunk wav, so the raw model output stays inspectable. This is also why the manifest gapBefore/gapAfter are LIVE for Higgs where they are inert for Orpheus.",
          "referenceSecondsCap": 30,
          "allowedControls": [],
          "_allowedControlsNote": "EMPTY ON PURPOSE, and it is a safety rule rather than a preference. An inline control token that is NOT in get_added_vocab() is split into 7-10 ordinary text pieces and READ ALOUD, which derails generation into a degenerate loop: ASR coverage 0.000, pitch std collapsing to 0.28 st, speaker cosine 0.05. A typo is not ignored — it destroys the render. v3 also has NO scene-description mechanism (<|scene_desc_start|> is a v2 chat role, absent from v3's vocabulary) and no `neutral`/`calm` emotion. At default sampling no control token beat plain text on speaker cosine, and the one candidate (<|prosody:long_pause|>) did not change measured pause density. The delivered render uses none. An empty allowlist means no engine ever has to get the validation right."
        },
        "mlx": {
          "_comment": "THE MAC'S OWN BLOCK. A CERTIFICATE IS PER (DIRECTORY, BACKEND), so this is not `served` copied across — every field here is stated for the mlx-audio sampler, which is a different implementation of top-k/top-p over a different runtime from vllm-omni's (PORT_NOTES 13.11). The loader picks the block by ARM (wsl -> served, darwin -> mlx) and refuses a fine-tune whose block carries a null cap.",
          "maxChars": 600,
          "maxCharsSource": "placeholder",
          "_maxCharsNote": "THE SAME PLACEHOLDER ON BOTH ARMS, AND THAT IS NOT AN INHERITANCE. A placeholder makes no claim about a backend — it is the zero-shot figure the v3 family was measured at on the served path, and it is labelled `placeholder` precisely so nobody reads it as a certificate. It may therefore be stated twice where a MEASURED number may not: narrator's own HiggsV3Defaults.MAX_CHARS is 600 on both arms too, so all three agree by construction rather than by copying. What must NEVER be stated twice is a length-sweep number, which is why deathstalker's mlx cap is null.",
          "edgeFadeMs": {
            "in": 10,
            "out": 25
          },
          "_edgeFadeNote": "SAME CODEC, SAME NUMBERS, and applied by the same code: narrator's assembler (assemble/engine_profiles.py declares higgs-v3 as pads=false with 10/25 ms raised-cosine fades) runs on whichever arm rendered. The fade is a property of the DECODED WAVEFORM's hard sample boundary, not of the sampler that produced the tokens, so unlike maxChars it genuinely does not vary by backend.",
          "referenceSecondsCap": 30,
          "_referenceSecondsCapNote": "The vllm-omni server's 30 s HTTP limit is enforced on THIS arm too, by narrator: HiggsV3MlxConfig calls v3_served.check_reference_budget, and HiggsV3Defaults.MAX_REFERENCE_SECONDS is 30.0 for both. Stated rather than omitted so a clips voice auditioned on the Mac is refused at the same length the server would refuse it at.",
          "allowedControls": []
        }
      },
      "note": "The model's own voice with no reference audio at all. MEASURED at ECAPA speaker cosine 0.0929 against the deathstalker narrator — 12% of that narrator's own 0.7656 self-consistency ceiling — i.e. it is a DIFFERENT SPEAKER, not a weak clone. It is listed because it is the one Higgs voice that needs nothing staged on disk, so it is what a machine can audition the serving stack with before any clone or fine-tune exists. Do not narrate a book in it expecting a particular narrator.",
      "_kindNote": "kind default — THE SERVED MODEL'S OWN VOICE, and a distinct shape rather than an empty clips list. narrator refuses a ClipsVoice with zero clips, and it is right to: a zero-shot clone with NO reference is not a degenerate clone, it is a different thing entirely — the model's built-in speaker, which measures at 12% of the deathstalker narrator's ECAPA ceiling. An earlier draft of this catalog wrote `clips: []` for it and called that 'the served default voice', which made the two indistinguishable in the wire format and would have been refused by name."
    },
    {
      "id": "deathstalker",
      "label": "Deathstalker (Higgs v3 fine-tune)",
      "kind": "checkpoint",
      "_targetCharsNote": "THE CHUNK SIZE THE CODE PACKS TO, PER ARM (backends.served.targetChars / backends.mlx.targetChars), set by the trainer after training from the training clips' text lengths. Owen, 2026-09-05: 'maxChars is what the model was trained to do, and targetChars can be what the system actually uses. maxChars is informative, targetChars is used by the code directly.' The prep's cap AND merge floor: consecutive short paragraphs travel together until the next would overflow it, a paragraph already at it stands alone, walls stay walls. Never above the arm's maxChars - both sides refuse that by name. A fine-tune with no targetChars cannot be prepped. INTERIM VALUES (Owen, 2026-09-05 evening: 'set it to deathstalker's theoretical maximum of about 80 seconds'): each arm's certified maximum - served 1200 (~71 s at the measured 5.9 audio-s per 100 chars; the served sweep dropped text at 1500), MLX 900 (its sweep dropped text at 1200). MEASURED the same evening on both arms: this checkpoint ENDS ITSELF at ~1,980 frames (~79-82 s) whatever the server ceiling - a 2,173-char render on the Mac gave 79 s with the middle skipped (coverage 0.306) - so no target above ~1,350 chars can work on this checkpoint; the retrain that favours longer clips states its own numbers here, with the method in targetCharsSource.",
      "engineVersion": "v3",
      "voice": {
        "checkpoint": {
          "wsl": "/home/telltale/higgs_v3_merged/ds_v7_prod",
          "darwin": "runtime/higgs-models/ds_v7_prod"
        }
      },
      "source": {
        "type": "hf",
        "ref": "owenmorgan/deathstalker-higgs-v3"
      },
      "_sourceNote": "WHERE A MACHINE DOWNLOADS THIS CHECKPOINT FROM (Settings -> Higgs -> Download). A PRIVATE HuggingFace repo under Owen's account, like the Orpheus voice repos, read with the HF token getHfToken() resolves (Settings, HF_TOKEN, ~/.config/bookforge/hf-owenmorgan.token). Uploaded 2026-09-06 from the PC's /home/telltale/higgs_v3_merged/ds_ad4lm_prod_ckpt1080 (weights, config, tokenizer, chat template, generation_config.json, merge_manifest.json, LICENSE) plus a NOTICE and a model card carrying the licence's required 'Derived from Higgs TTS 3, licensed from Boson AI USA, Inc.' statement. The download lands in THIS ARM's voice.checkpoint path (wsl: inside the guest via the higgs3 env; darwin: natively under userData) and higgs_download.py refuses a copy that lacks generation_config.json. Same bytes as the certificate above was issued for: the cap stays certified for a downloaded copy ONLY as long as the repo holds the ckpt-1080 merge - re-uploading a different merge here without re-certifying is a lie about the cap.",
      "_checkpointArmNote": "TWO ARMS, TWO COPIES, same basename ds_v7_prod. wsl: /home/telltale/higgs_v3_merged/ds_v7_prod. darwin: the SAME directory rsynced to ~/Library/Application Support/BookForge/runtime/higgs-models/ds_v7_prod, sha-verified per weight file. Promoted 2026-09-10 after the --tail-s 0.25 retrain fixed the EOS failure that made v6 unusable (0% runaway (0/24 probe); field notes 4n.53/4n.54). PREVIOUS: wsl ds_v5_prod, darwin ds_ad4lm_prod_ckpt1080. WSL ARM (2026-09-07 promotion): /home/telltale/higgs_v3_merged/ds_v5_prod -- the ds_v5 train's ladder pick (ckpt-1102), sha256 5a9ab007...; DARWIN ARM: runtime/higgs-models/ds_ad4lm_prod_ckpt1080 -- the PREVIOUS merge, still what the Mac renders (sha 218afc55... = the HF upload). The two arms serve DIFFERENT weights until the Mac is promoted; see _promotionNote. TWO ARMS, TWO COPIES, TWO CERTIFICATES — and this entry names both because a checkpoint voice is ~8.5 GB on disk and the arms cannot see each other's disks. `wsl` is the merged directory inside the WSL guest, which is what serve_higgs_v3.sh is started on; `darwin` is the SAME merged directory copied onto the Mac, written RELATIVE to the app's userData (it resolves to /Users/<account>/Library/Application Support/BookForge/runtime/higgs-models/ds_ad4lm_prod_ckpt1080, beside the `base` weights the MLX backend already loads from). The Mac copy was staged 2026-09-05 and carries the same file set (model.safetensors, config.json, tokenizer.json, tokenizer_config.json, chat_template.jinja, generation_config.json, merge_manifest.json). WHY THE darwin ENTRY IS RELATIVE AND THE wsl ONE IS NOT: the guest has a fixed home and hands its path straight to a launch script, while a Mac's Application Support path carries the ACCOUNT NAME — an absolute /Users/telltale/... in a repo-tracked catalog is a directory that exists on exactly one machine, which is the failure this catalog exists to prevent (see _cloneFallbackNote for the same rule applied to reference clips). The app knows its own userData; the catalog does not, so higgsVoicesDocument() joins them at document-write time and the wire format still carries ONE absolute `checkpointDir`. AN ARM WITH NO ENTRY IS NOT A GAP TO FILL FROM THE OTHER ONE: the voice is simply not loadable there, refused by name ('is not staged for the Mac: no darwin checkpoint in the catalog'), and the narration dropdown greys it out on that machine. AND A COPY IS A NEW CERTIFICATE, NOT AN INHERITED ONE — see backends: the cap is per (directory, BACKEND), and the MLX sampler is a different implementation of top-k/top-p over a different runtime from vllm-omni's, so the same three numbers make the configuration identical and not the draws (PORT_NOTES 13.11).",
      "_checkpointDirNote": "wsl: /home/telltale/higgs_v3_merged/ds_v7_prod - the v7 retrain (ckpt-744, holdout 3.8353), merged 2026-09-10. It is NOT the higgs-models naming convention because a certificate is bound to the exact directory that was rendered against, so a re-merge is a new directory and a new certificate. PREVIOUS wsl dir: ds_v5_prod. PREVIOUS darwin dir: ds_ad4lm_prod_ckpt1080. NOT the /home/<user>/higgs-models/<voice>/ convention the installer creates, and NOT interchangeable with the sibling directory beside it. A CERTIFICATE BINDS (checkpoint dir, stage-processor patch sha, max_chars) TOGETHER: a cap is measured by rendering against ONE directory on ONE patched server, so it is that triple that is certified and never the number alone. Two merges of the same run are two directories and two certificates. WHAT IS ON DISK (2026-09-05): /home/telltale/higgs_v3_merged/ds_ad4lm_prod_ckpt1080 — ckpt-1080, the LOWEST-LOSS checkpoint, chosen by Owen after an ear test, and what this entry names; CERTIFIED 2026-09-05T12:52:57 at max_chars 1200 against this directory, its generation_config.json and the patched stage processor 0b36f650 — certificate at E:\\training\\_campaigns\\2026-09-01-cod-full-rebuild\\higgs\\v3_ft\\logs\\max_chars_certificate_ckpt1080.json. And /home/telltale/higgs_v3_merged/ds_ad4lm_prod — ckpt-480, the rule-picked one, kept as the ALTERNATE and carrying its own certificate (max_chars 1200, certified 2026-09-05 against that directory and the patched stage processor 0b36f650…). THAT CERTIFICATE IS NOT THIS DIRECTORY'S: nothing here, in narrator or in vllm-omni compares two directories' weights or generation_config.json byte for byte, so a number carried across would be inherited without its measurement. BOTH ARE FROZEN: a re-merge or a recipe change writes a NEW directory name and re-certifies; since 2026-09-05 a merged directory may not be RENAMED either, and certification asserts that a directory's merge_manifest.out names ITSELF. HIGGS_MODELS_DIR in install_higgs_env.sh still describes where a voice MAY be staged; it is not where this one is, and moving it would mean running the sweep again. PROVENANCE CORRECTION, kept as history because a stale path string is exactly how two merged directories stop being distinguishable. FOUND 2026-09-05: this directory's merge_manifest.json recorded \"out\": \"/home/telltale/higgs_v3_merged/ds_ad4lm_prod\" — its SIBLING's path — and its generation_config.json said \"_written_by\": \"backfilled 2026-09-05 for ds_ad4lm_prod\". CAUSE: it was merged TO the path ds_ad4lm_prod and RENAMED to ds_ad4lm_prod_ckpt1080 when ckpt-480 took the ds_ad4lm_prod name — so the path strings were stale, and only the path strings. CORRECTED IN PLACE 2026-09-05T10:35:56 by the training side: both files now name THIS directory, each carries a timestamped `provenance_correction` recording the rename, and the original value is KEPT rather than overwritten (merge_manifest `out_original_at_merge_time`). CONFIRMED by re-reading both files read-only, 2026-09-05: `out` and `_written_by` name ds_ad4lm_prod_ckpt1080, `lora` is still runs/ds_ad4lm_prod/ckpt-1080 on snapshot 239f63fb…, sampling is still 1.0 / 0.95 / 50 / 1.0, and model.safetensors' mtime is unchanged (08:08) against the two JSONs' 10:35 — the weights were not touched. The two guards above are what came out of it; the catalog path is still what says which weights render, but it is no longer the ONLY thing, because the directory now names itself.",
      "license": "boson-higgs-tts-3-research-noncommercial",
      "commercialUse": false,
      "sampleRate": 24000,
      "addedAt": "2026-09-04",
      "backends": {
        "served": {
          "maxChars": 800,
          "maxCharsSource": "catalog",
          "safeMinChars": 600,
          "safeMaxChars": 800,
          "_safeBandNote": "SAFE BAND = the training corpus's INTERQUARTILE RANGE, measured 2026-09-09 (field notes 4n.37.20). ds_v5: p25 598, p75 1094 -> floor 600. Validated on thirdreich, whose IQR 532-985 predicted the measured best band 600-1000 (4/80 = 5.0% early stops [2.0-12.2], vs 18.8% below 600 and 20.3% above 1000). safeMaxChars is held at Owen's 800 ruling rather than the measured 1000; the measurement supports raising it. safeMinChars is the NEW half: before it, floor==cap==targetChars, so two 400-char paragraphs could not merge (800 > 700 cap) and a 400-char chunk shipped - which is how Owen's 2026-09-09 Mac render truncated at 323, 502 and 634 chars.",
          "_maxCharsNote": "800, OWEN'S RULING (2026-09-09): \"my models are limited to an absolute max of 800 characters before we start getting truncations\" - the cap is 800 on every fine-tune, both arms. PREVIOUS: SUPERSEDED DIRECTORY (2026-09-07): everything after 'ORIGINAL NOTE' certified 1200 for ds_ad4lm_prod_ckpt1080 by a length sweep; that directory was deleted from this PC on the ds_v5_prod promotion, and the served arm's number is now 1764 by the training-ceiling rule in _maxCharsSourceNote. Kept as the record of how a certificate is made and what it binds to. ORIGINAL NOTE: 1200, CERTIFIED for THIS directory — /home/telltale/higgs_v3_merged/ds_ad4lm_prod_ckpt1080 — on 2026-09-05T12:52:57 by the Higgs fine-tuning session. Certificate: E:\\training\\_campaigns\\2026-09-01-cod-full-rebuild\\higgs\\v3_ft\\logs\\max_chars_certificate_ckpt1080.json (read read-only when this was written). THE RULE, quoted from the file: \"babble==0 across all seeds AND min per-seed coverage>=0.90, contiguous from the shortest tested length\". Scored by ASR alignment and never by duration ratio — a v3 render measured ratio 0.99 while dropping 22 % of its text. THE LADDER, from the certificate's contiguous_pass: 150, 300, 600, 900, 1200 — five lengths passing without a gap, and 1200 is the last of them. PER-LENGTH FIGURES reported by the session with the number: babble 0/4 seeds at 600 / 900 / 1200, minimum per-seed coverage 99.2 % / 97.1 % / 97.3 %; 1500 FAILS at 86.1 % — on COVERAGE, not on babble, which is the same shape the ckpt-480 sweep found. WHAT THE NUMBER IS BOUND TO, all of it recorded in the certificate's bound_to: the checkpoint dir above (whose merge_manifest.out names ITSELF — asserted at certify time), lora runs/ds_ad4lm_prod/ckpt-1080 on base snapshot 239f63fb…, the directory's generation_config.json {temperature 1.0, top_p 0.95, top_k 50, repetition_penalty 1.0}, and THE SERVER BUILD: stage-input-processor sha256 0b36f6507dd11653, the token-identity sentinel filter (patch_sentinel_filter.py), vllm/vllm-omni 0.28.0. Change any of those and this number is not certified for what is running — see _checkpointDirNote. OBSERVED, and recorded rather than smoothed over: the certifying server ran with --max-num-seqs 64 while this catalog's shared serving block declares maxNumSeqs 2. Nothing measured says whether batch width moves the safe chunk length; it is stated so that a reader comparing the two does not assume they were the same server. WHY THIS FIELD MAY NEVER BE INHERITED: a fine-tuned Higgs checkpoint's stop length tracks its TRAINING CLIP LENGTH rather than the text it is given — a 30-minute adapter trained on 8-22 s clips stopped after ~6-10 s of audio on any prompt over ~150 characters — so the zero-shot 600 is wrong for a fine-tune in the direction that LOSES TEXT while every duration check still looks plausible. The sibling directory ds_ad4lm_prod (ckpt-480) is certified at 1200 too; that the two agree is a RESULT, not a reason — each was measured against its own weights.",
          "edgeFadeMs": {
            "in": 10,
            "out": 25
          },
          "referenceSecondsCap": 30,
          "allowedControls": [],
          "_maxCharsSourceNote": "'catalog' = declared here from the training data, not measured by a length sweep. OWEN'S RULE (2026-09-07): 'max chunk is what we trained on' -- the served max is the LONGEST TRAINING ROW of the directory's own corpus. ds_v5 mix (build.log): 943 train rows, chars p50 806 / p75 1092 / p90 1390 / MAX 1764; seconds p50 50 / p90 87 / max 100; 181 rows (19.2%) at or above 1200. Ladder evidence on ds_v5_prod at 0.8/0.95/50: 1110-1265-char prose 7/8 clean (one early stop at 1265); nothing was rendered between 1265 and 2022, and the two 2022-char renders early-stopped. The narrator accepts only catalog | placeholder | length-sweep in maxCharsSource (a prose value failed prep once on 2026-09-07)."
        },
        "mlx": {
          "_comment": "THE MAC'S OWN BLOCK FOR THIS FINE-TUNE, and it is UNCERTIFIED. The merged directory was copied to the Mac on 2026-09-05 (sha256-verified against the frozen WSL dir, same basename) -- see voice.checkpoint.darwin -- and the weights being identical is exactly why this block has to exist separately: a cap is produced by RENDERING, and the render here goes through mlx-audio's sampler rather than vllm-omni's.",
          "maxChars": 800,
          "maxCharsSource": "catalog",
          "safeMinChars": 600,
          "safeMaxChars": 800,
          "_safeBandNote": "SAFE BAND = the training corpus's INTERQUARTILE RANGE, measured 2026-09-09 (field notes 4n.37.20). ds_v5: p25 598, p75 1094 -> floor 600. Validated on thirdreich, whose IQR 532-985 predicted the measured best band 600-1000 (4/80 = 5.0% early stops [2.0-12.2], vs 18.8% below 600 and 20.3% above 1000). safeMaxChars is held at Owen's 800 ruling rather than the measured 1000; the measurement supports raising it. safeMinChars is the NEW half: before it, floor==cap==targetChars, so two 400-char paragraphs could not merge (800 > 700 cap) and a 400-char chunk shipped - which is how Owen's 2026-09-09 Mac render truncated at 323, 502 and 634 chars.",
          "_maxCharsNote": "800, OWEN'S RULING (2026-09-09): \"my models are limited to an absolute max of 800 characters before we start getting truncations\" - the cap is 800 on every fine-tune, both arms. PREVIOUS: 900, CERTIFIED for THIS directory ON THIS BACKEND - the Mac copy of ds_ad4lm_prod_ckpt1080 (sha256-identical to the WSL dir the served certificate binds to) rendered through narrator's own HiggsV3MlxEngine (mlx-audio 0.4.8, mlx 0.32.0, owens-mac-studio) on 2026-09-05T13:19 by the same rule and the same scorer objects as the served certificate (faster-whisper base.en int8 + difflib coverage; babble = coverage < 0.05; babble==0 across all seeds AND min per-seed coverage >= 0.90, contiguous from the shortest): seeds 500-503 x 150/300/600/900/1200/1500; babble 0/4 at every length; min coverage 100.0 / 97.3 / 98.5 / 94.8 at 150/300/600/900, then 1200 FAILS at 83.2% (seed 500 dropped a middle block; the other three seeds passed) and 1500 at 84.4%. The failure mode on MLX is DROPPED TEXT, never babble. NOT the served number: the served path certified 1200 on the identical weights, which is exactly why a certificate is per (directory, backend). Full per-seed table, RTF (0.52-0.62), peak memory (11.3 GiB at 900) and every deviation from the served harness: docs/certificates/max_chars_certificate_mlx_ckpt1080.json.",
          "edgeFadeMs": {
            "in": 10,
            "out": 25
          },
          "referenceSecondsCap": 30,
          "allowedControls": []
        }
      },
      "pace": {
        "median": 16.64,
        "mean": 16.78,
        "p05": 15.73,
        "p95": 18.02,
        "p99": 18.26,
        "n": 40,
        "method": "chars = len(s1-normalized bank text, no markers); seconds = full wav duration as written, no trim; SAMPLING 0.8 / 0.95 / 50 (the engine-level catalog sampling since 5676002a); clean_rule (the ladder's, verbatim): ok and coverage >= 0.95 and audio_s > 2 and chars >= 150 and cls not in (micro, heading) and 0.8 <= audio_ratio <= 1.25 and no early_stop/run_on/babble mode",
        "source": "served arm at temperature 0.8 on ds_v5_prod: night3/ds5/para (Fuhrer-49 bank, 50 renders, 40 clean; pace_deathstalker_served.json carries p1 15.51 / p5 15.73 / median 16.64 / mean 16.78 / p95 18.02 / p99 18.26, n_clean, clean_rule, band_rule). Replaces the ds_ad4lm_prod_ckpt1080 block (median 16.73, p99 18.21, n 73) on the 2026-09-07 WSL promotion. Seed band by BookForge's rule (2026-09-08): max = median x 1.2 = 19.97, min = median / 1.3 = 12.8 - a SEED whose RATIOS narrator re-centres on the book's own running median, not a fixed ceiling. One pace per voice, both arms -- note the Mac still serves the old directory.",
        "measuredOn": "2026-09-07"
      },
      "_paceNote": "THE VOICE'S MEASURED PACE, chars of text per second of audio, ONE PER VOICE for both arms (Owen 2026-09-06: 'it can be the same setting for both mac and windows since theyre the same voice and theyre pretty close'). From the training ladder's CLEAN renders (coverage >= 0.95, no early stop, no babble, 0.8 <= audio ratio <= 1.25, chars > 150). THE LENGTH GUARD'S SEED BAND IS DERIVED FROM THE MEDIAN AND THEN RE-CENTRED ON THE BOOK (2026-09-08): paceCharsPerSec = median = 16.64, maxCharsPerSec = median x 1.2 = 19.97 (above it a take is too SHORT - an early stop) and minCharsPerSec = median / 1.3 = 12.8 (below it a take RAN ON - a babble tail). ALL THREE ARE A SEED, NOT A CEILING: narrator keeps only the band's RATIOS and re-centres them on the running median of the book's own shipped takes once ten guarded chunks are in (truncation.PaceTracker), which is what actually answers Owen's worry - 'if its set to 17.11 chars/s, and one book averages 17.6/s, it shouldnt split and re-render everything over 17.11' - because a book that averages 17.6 BECOMES the reference and both edges move with it. The rule this replaces (the ladder's p99 x 1.15 and p05 / 1.15) could not do that, and MEASURED on the first full book rendered under it (Shift, mistborn, 1,313 chunks, 16.38 h, 2026-09-08) it was the wrong shape: the ladder bank is uniform nonfiction and Shift is fiction with dialogue, so the book's clean chunks paced 14.09 chars/s against the bank's 15.12 - the fixed long edge (11.97) fired on 340 healthy chunks at take 0 and the ladder re-rolled or split every one of them, while the short edge (19.27) sat at 1.37x the book's own pace and shipped 8 spot-aligned truncations (13-52 % of the words dropped, all at >= 1.23x the book's pace). The factors 1.2 and 1.3 are BookForge's PACE_GUARD_SHORT_FACTOR and PACE_GUARD_LONG_FACTOR. Files: E:\\\\training\\\\_campaigns\\\\2026-09-01-cod-full-rebuild\\\\higgs\\\\night3\\\\pace\\\\*.json. A voice with no pace block renders at narrator's default band (20 / 14.5, the Fuhrer whole-book measurement on deathstalker).",
      "note": "The deathstalker narrator as a merged Higgs v3 fine-tune. WHY A FINE-TUNE RATHER THAN REFERENCE CLIPS: zero-shot cloning already recovers 92% of the narrator's speaker-identity ceiling (cosine 0.7037 of 0.7656, from a 27.4 s two-clip same-book reference), so TIMBRE is largely solved without training. What cloning does NOT recover is PHRASING — the render places 2.01 pauses per 100 chars against the narrator's 1.39 and runs pitch std 5.17 st against his 4.36, i.e. it performs the text rather than reading it, and no reference, sampling setting or control token moved either number. Only a fine-tune on his phrasing closes that gap, which is what this entry is for — and it is also why Owen ruled on 2026-09-04 that production narration is fine-tuned voices ONLY.",
      "_cloneFallbackNote": "There is deliberately no clips-based deathstalker entry standing in for this one. The measured best zero-shot configuration is a 27.4 s join of cd_cd_00001100.wav + cd_cd_00001951.wav from the ds_ad4s cut with their book-exact transcripts, and it renders well — but those clips live under E:\\training and are not a deployable artifact location, so an entry pointing at them would be a voice that works on exactly one machine, which is the failure this catalog exists to prevent. To add one: join the clips to a single wav (0.35 s of silence between them), join the transcripts in the same order, stage both under /home/<user>/higgs-models/<voice>/refs/, and declare ONE clip here with its measured `seconds`.",
      "_kindNote": "kind 'checkpoint' — a MERGED fine-tune directory the server is started on, prompted TEXT-ONLY. No clips key: the voice is in the weights. It was called 'adapter' until 2026-09-04, which described the artifact we trained rather than the artifact that SERVES: vllm-omni has no runtime LoRA path, so the LoRA is an archival input to a merge and never a catalog field.",
      "_promotionNote": "PROMOTED 2026-09-10 11:20 to ds_v7_prod (ckpt-744, holdout 3.8353). THE RETRAIN THAT MADE THIS SHIPPABLE: the previous v6 model was unusable - it did not emit EOS and ran to the token cap on a large share of renders (mistborn 58%, deathstalker 29%) with a median 37.9 s of trailing silence, caused by RETAINED post-chunk pauses (slice_vtt --tail-s 4.0). Re-sliced at --tail-s 0.25 and retrained: 0% runaway on the probe; ladder band measures 400-1000 at 3.6% but is HELD at 600-800 by ruling. Owen Morgan v7 was the positive control (0/24, same corpus, only the tail changed). Field notes 4n.53/4n.54. Merged dir sha256_16 02cfa1ff75bc3bf4. PREVIOUS ARM DIRS: wsl /home/telltale/higgs_v3_merged/ds_v7_prod, darwin runtime/higgs-models/ds_v7_prod. HuggingFace deliberately NOT updated - Owen has not given a green light."
    },
    {
      "id": "mistborn",
      "label": "Mistborn (Higgs v3 fine-tune)",
      "kind": "checkpoint",
      "_targetCharsNote": "THE CHUNK SIZE THE CODE PACKS TO, PER ARM (backends.served.targetChars / backends.mlx.targetChars), set by the trainer after training from the training clips' text lengths. Owen, 2026-09-05: 'maxChars is what the model was trained to do, and targetChars can be what the system actually uses. maxChars is informative, targetChars is used by the code directly.' The prep's cap AND merge floor: consecutive short paragraphs travel together until the next would overflow it, a paragraph already at it stands alone, walls stay walls. Never above the arm's maxChars - both sides refuse that by name. A fine-tune with no targetChars cannot be prepped. INTERIM VALUES (Owen, 2026-09-05 evening: 'set it to deathstalker's theoretical maximum of about 80 seconds'): each arm's certified maximum - served 1200 (~71 s at the measured 5.9 audio-s per 100 chars; the served sweep dropped text at 1500), MLX 900 (its sweep dropped text at 1200). MEASURED the same evening on both arms: this checkpoint ENDS ITSELF at ~1,980 frames (~79-82 s) whatever the server ceiling - a 2,173-char render on the Mac gave 79 s with the middle skipped (coverage 0.306) - so no target above ~1,350 chars can work on this checkpoint; the retrain that favours longer clips states its own numbers here, with the method in targetCharsSource.",
      "engineVersion": "v3",
      "voice": {
        "checkpoint": {
          "wsl": "/home/telltale/higgs_v3_merged/mb_v7_prod",
          "darwin": "runtime/higgs-models/mb_v7_prod"
        }
      },
      "_checkpointArmNote": "TWO ARMS, TWO COPIES, same basename mb_v7_prod. wsl: /home/telltale/higgs_v3_merged/mb_v7_prod. darwin: the SAME directory rsynced to ~/Library/Application Support/BookForge/runtime/higgs-models/mb_v7_prod, sha-verified per weight file. Promoted 2026-09-10 after the --tail-s 0.25 retrain fixed the EOS failure that made v6 unusable (4.5% long at pace 13.3, vs the thirdreich reference 5.2%; field notes 4n.53/4n.54). PREVIOUS: wsl mb_h2lm_prod, darwin mb_h2lm_prod. TWO ARMS, TWO COPIES. wsl: /home/telltale/higgs_v3_merged/mb_h2lm_prod -- the merged HF directory re-made on 2026-09-08 from night2/runs/mb_h2lm/ckpt-920 (the dip; holdout 3.73813; 746 train rows, long tier 536 clips 15-149 s + 210 short + 45 micro). darwin: the SAME directory rsynced to the Mac under BookForge's app-support runtime (~/Library/Application Support/BookForge/runtime/higgs-models/mb_h2lm_prod), sha-verified. This is a RESTORATION: mb_v3_prod held this slot 2026-09-08 01:2x-14:xx and was retired by ear.",
      "_checkpointDirNote": "wsl: /home/telltale/higgs_v3_merged/mb_v7_prod - the v7 retrain (ckpt-704, holdout 3.9137), merged 2026-09-10. It is NOT the higgs-models naming convention because a certificate is bound to the exact directory that was rendered against, so a re-merge is a new directory and a new certificate. PREVIOUS wsl dir: mb_h2lm_prod. PREVIOUS darwin dir: mb_h2lm_prod. wsl: /home/telltale/higgs_v3_merged/mb_h2lm_prod is what the served arm renders; darwin: runtime/higgs-models/mb_h2lm_prod is what the MLX arm renders. A re-merge under the same name is a different model and re-measures; this one is byte-identical in provenance to the 2026-09-06 promotion (same ckpt-920, same merge script), so its 2026-09-06/07 measurements still stand.",
      "license": "boson-higgs-tts-3-research-noncommercial",
      "commercialUse": false,
      "sampleRate": 24000,
      "addedAt": "2026-09-06",
      "backends": {
        "served": {
          "maxChars": 800,
          "maxCharsSource": "catalog",
          "safeMinChars": 500,
          "safeMaxChars": 800,
          "_safeBandNote": "SAFE BAND = the training corpus's INTERQUARTILE RANGE, measured 2026-09-09 (field notes 4n.37.20). mb_h2lm: p25 281, p75 893 -> floor 300. Validated on thirdreich, whose IQR 532-985 predicted the measured best band 600-1000 (4/80 = 5.0% early stops [2.0-12.2], vs 18.8% below 600 and 20.3% above 1000). safeMaxChars is held at Owen's 800 ruling rather than the measured 1000; the measurement supports raising it. safeMinChars is the NEW half: before it, floor==cap==targetChars, so two 400-char paragraphs could not merge (800 > 700 cap) and a 400-char chunk shipped - which is how Owen's 2026-09-09 Mac render truncated at 323, 502 and 634 chars.",
          "_maxCharsNote": "800, OWEN'S RULING (2026-09-09): \"my models are limited to an absolute max of 800 characters before we start getting truncations\" - the cap is 800 on every fine-tune, both arms. PREVIOUS: 1200. NOTE THE DEVIATION FROM 'max == the longest training row': mb_h2lm's longest training row is 2268 chars / 149 s, but this checkpoint ENDS ITSELF at ~1,980 frames (~79-82 s) whatever the server ceiling -- a 2,173-char render on the Mac returned 79 s with the middle skipped (coverage 0.306), measured 2026-09-05. At the measured pace of 15.01 chars/s, 82 s is ~1,230 chars, so 1200 is the empirical ceiling and 2268 would ship a config the model cannot honour. The rule is 'the max chunk is what the model trained on' only where the model can render it.",
          "edgeFadeMs": {
            "in": 10,
            "out": 25
          },
          "referenceSecondsCap": 30,
          "allowedControls": [],
          "_maxCharsSourceNote": "'catalog' = declared here, backed by the training session's mb2 measurement on SGLang-Omni at 16 wide (50 Fuhrer chunks packed at 1200: 0 early stops, 0 gibberish, 6/50 damaged) plus the self-termination measurement above; not a length sweep."
        },
        "mlx": {
          "_comment": "THE MAC'S OWN BLOCK FOR THIS FINE-TUNE, UNMEASURED on mb_v3_prod: 900 mirrors deathstalker's MLX certificate as the conservative number for the same architecture; `placeholder` says nobody has measured THIS voice on this arm. Replace both numbers and the source after a Mac sweep.",
          "maxChars": 800,
          "maxCharsSource": "catalog",
          "safeMinChars": 500,
          "safeMaxChars": 800,
          "_safeBandNote": "SAFE BAND = the training corpus's INTERQUARTILE RANGE, measured 2026-09-09 (field notes 4n.37.20). mb_h2lm: p25 281, p75 893 -> floor 300. Validated on thirdreich, whose IQR 532-985 predicted the measured best band 600-1000 (4/80 = 5.0% early stops [2.0-12.2], vs 18.8% below 600 and 20.3% above 1000). safeMaxChars is held at Owen's 800 ruling rather than the measured 1000; the measurement supports raising it. safeMinChars is the NEW half: before it, floor==cap==targetChars, so two 400-char paragraphs could not merge (800 > 700 cap) and a 400-char chunk shipped - which is how Owen's 2026-09-09 Mac render truncated at 323, 502 and 634 chars.",
          "_maxCharsNote": "800, OWEN'S RULING (2026-09-09): \"my models are limited to an absolute max of 800 characters before we start getting truncations\" - the cap is 800 on every fine-tune, both arms. PREVIOUS: 900, a PLACEHOLDER borrowed from deathstalker's MLX certificate; not measured on mb_v3_prod.",
          "edgeFadeMs": {
            "in": 10,
            "out": 25
          },
          "referenceSecondsCap": 30,
          "allowedControls": []
        }
      },
      "pace": {
        "median": 15.01,
        "mean": 14.95,
        "p05": 13.6,
        "p95": 16.15,
        "p99": 16.78,
        "n": 56,
        "method": "as deathstalker",
        "measuredOn": "2026-09-07",
        "source": "served arm at temperature 0.8: night3/pace08/mistborn/t08 (100 renders, 56 clean), measured on THIS directory (mb_h2lm_prod). All six numbers are the ladder's own (p1 12.72 / p5 13.60 / median 15.01 / mean 14.95 / p95 16.15 / p99 16.78, clean_rule and band_rule inside); BookForge's recomputation matched p99 to the hundredth. Restored 2026-09-08 with the checkpoint."
      },
      "_paceNote": "THE VOICE'S MEASURED PACE, chars of text per second of audio, ONE PER VOICE for both arms (Owen 2026-09-06: 'it can be the same setting for both mac and windows since theyre the same voice and theyre pretty close'). From the training ladder's CLEAN renders (coverage >= 0.95, no early stop, no babble, 0.8 <= audio ratio <= 1.25, chars > 150). THE LENGTH GUARD'S SEED BAND IS DERIVED FROM THE MEDIAN AND THEN RE-CENTRED ON THE BOOK (2026-09-08): paceCharsPerSec = median = 15.12, maxCharsPerSec = median x 1.2 = 18.14 (above it a take is too SHORT - an early stop) and minCharsPerSec = median / 1.3 = 11.63 (below it a take RAN ON - a babble tail). ALL THREE ARE A SEED, NOT A CEILING: narrator keeps only the band's RATIOS and re-centres them on the running median of the book's own shipped takes once ten guarded chunks are in (truncation.PaceTracker), which is what actually answers Owen's worry - 'if its set to 17.11 chars/s, and one book averages 17.6/s, it shouldnt split and re-render everything over 17.11' - because a book that averages 17.6 BECOMES the reference and both edges move with it. The rule this replaces (the ladder's p99 x 1.15 and p05 / 1.15) could not do that, and MEASURED on the first full book rendered under it (Shift, mistborn, 1,313 chunks, 16.38 h, 2026-09-08) it was the wrong shape: the ladder bank is uniform nonfiction and Shift is fiction with dialogue, so the book's clean chunks paced 14.09 chars/s against the bank's 15.12 - the fixed long edge (11.97) fired on 340 healthy chunks at take 0 and the ladder re-rolled or split every one of them, while the short edge (19.27) sat at 1.37x the book's own pace and shipped 8 spot-aligned truncations (13-52 % of the words dropped, all at >= 1.23x the book's pace). The factors 1.2 and 1.3 are BookForge's PACE_GUARD_SHORT_FACTOR and PACE_GUARD_LONG_FACTOR. Files: E:\\\\training\\\\_campaigns\\\\2026-09-01-cod-full-rebuild\\\\higgs\\\\night3\\\\pace\\\\*.json. A voice with no pace block renders at narrator's default band (20 / 14.5, the Fuhrer whole-book measurement on deathstalker). NOTE: mistborn paces 11 % slower than deathstalker (p05 13.38 sits UNDER the 14.5 default long edge), which is why the band is per voice.",
      "note": "The Mistborn narrator as a merged Higgs v3 fine-tune (mb_h2lm, the second mistborn train, dip checkpoint 920), cut from the ADOBE-ENHANCED, DIALOGUE-FREE Kramer narration spans. Restored 2026-09-08 as the definitive voice after mb_v3 -- a 27 h corpus that mixed those Adobe masters with roformer-denoised full books -- was rejected on long-form listening: Owen, 'prosody is good but the voices/clips we used definitely werent normalized via adobe. i can hear quality differences between them.' More data did not smooth the source step out, so the dataset rule for this voice is now Adobe-cleaned material only, narration only, text AI-cleaned with the BookForge CLI, then aligned.",
      "_kindNote": "kind 'checkpoint' — a MERGED fine-tune directory the server is started on, prompted TEXT-ONLY. No clips key: the voice is in the weights. It was called 'adapter' until 2026-09-04, which described the artifact we trained rather than the artifact that SERVES: vllm-omni has no runtime LoRA path, so the LoRA is an archival input to a merge and never a catalog field.",
      "source": {
        "type": "hf",
        "ref": "owenmorgan/mistborn-higgs-v3"
      },
      "_sourceNote": "WHERE A MACHINE DOWNLOADS THIS CHECKPOINT FROM (Settings -> Higgs -> Download): a PRIVATE HuggingFace repo under Owen's account. Re-uploaded 2026-09-08 with the mb_h2lm weights, replacing the mb_v3 weights that briefly held the repo; see _promotionNote for the sha256 a downloaded copy must match.",
      "_promotionNote": "PROMOTED 2026-09-10 11:20 to mb_v7_prod (ckpt-704, holdout 3.9137). THE RETRAIN THAT MADE THIS SHIPPABLE: the previous v6 model was unusable - it did not emit EOS and ran to the token cap on a large share of renders (mistborn 58%, deathstalker 29%) with a median 37.9 s of trailing silence, caused by RETAINED post-chunk pauses (slice_vtt --tail-s 4.0). Re-sliced at --tail-s 0.25 and retrained: 4.5% long at its own pace 13.3 (thirdreich reference measures 5.2%); trailing silence median 0.22 s. Owen Morgan v7 was the positive control (0/24, same corpus, only the tail changed). Field notes 4n.53/4n.54. Merged dir sha256_16 11338e6474c661e3. PREVIOUS ARM DIRS: wsl /home/telltale/higgs_v3_merged/mb_v7_prod, darwin runtime/higgs-models/mb_v7_prod. HuggingFace deliberately NOT updated - Owen has not given a green light."
    },
    {
      "id": "owen",
      "label": "Owen Morgan (Higgs v3 fine-tune)",
      "kind": "checkpoint",
      "_targetCharsNote": "THE CHUNK SIZE THE CODE PACKS TO, PER ARM (backends.served.targetChars / backends.mlx.targetChars), set by the trainer after training from the training clips' text lengths. Owen, 2026-09-05: 'maxChars is what the model was trained to do, and targetChars can be what the system actually uses. maxChars is informative, targetChars is used by the code directly.' The prep's cap AND merge floor: consecutive short paragraphs travel together until the next would overflow it, a paragraph already at it stands alone, walls stay walls. Never above the arm's maxChars - both sides refuse that by name. A fine-tune with no targetChars cannot be prepped. INTERIM VALUES (Owen, 2026-09-05 evening: 'set it to deathstalker's theoretical maximum of about 80 seconds'): each arm's certified maximum - served 1200 (~71 s at the measured 5.9 audio-s per 100 chars; the served sweep dropped text at 1500), MLX 900 (its sweep dropped text at 1200). MEASURED the same evening on both arms: this checkpoint ENDS ITSELF at ~1,980 frames (~79-82 s) whatever the server ceiling - a 2,173-char render on the Mac gave 79 s with the middle skipped (coverage 0.306) - so no target above ~1,350 chars can work on this checkpoint; the retrain that favours longer clips states its own numbers here, with the method in targetCharsSource.",
      "engineVersion": "v3",
      "voice": {
        "checkpoint": {
          "wsl": "/home/telltale/higgs_v3_merged/ow_v7_prod",
          "darwin": "runtime/higgs-models/ow_v7_prod"
        }
      },
      "source": {
        "type": "hf",
        "ref": "owenmorgan/owen-morgan-higgs-v3"
      },
      "_sourceNote": "WHERE A MACHINE DOWNLOADS THIS CHECKPOINT FROM (Settings -> Higgs -> Download): a PRIVATE HuggingFace repo under Owen's account, uploaded from the WSL directory on the 2026-09-07 promotion (see _promotionNote for the sha256 that a downloaded copy must match).",
      "_checkpointArmNote": "TWO ARMS, TWO COPIES, same basename ow_v7_prod. wsl: /home/telltale/higgs_v3_merged/ow_v7_prod. darwin: the SAME directory rsynced to ~/Library/Application Support/BookForge/runtime/higgs-models/ow_v7_prod, sha-verified per weight file. Promoted 2026-09-10 after the --tail-s 0.25 retrain fixed the EOS failure that made v6 unusable (0/24 runaway; field notes 4n.53/4n.54). PREVIOUS: wsl owen_gp2w_prod, darwin owen_gp2w_prod. TWO ARMS, TWO COPIES. wsl: /home/telltale/higgs_v3_merged/owen_gp2w_prod -- the merged HF directory the training session wrote on the PC (owen_gp2w train: God's People paragraphs as the long tier + God's People and WHAA sentences as the short tier, 411 rows / 7.8 h; early stop at step 475; ladder pick ckpt-425; 8.0 GB; generation_config.json T1.0/top_p 0.95/top_k 50 written at run creation, overridden per request by the engine-level 0.8/0.95/50). darwin: the SAME directory copied to the Mac under BookForge's app-support runtime (~/Library/Application Support/BookForge/runtime/higgs-models/owen_gp2w_prod) on 2026-09-07 -- rsync from WSL, sha256-verified against the WSL dir; until it lands the MLX arm refuses this voice by name, which is the honest state.",
      "_checkpointDirNote": "wsl: /home/telltale/higgs_v3_merged/ow_v7_prod - the v7 retrain (ckpt-966, holdout 3.8899), merged 2026-09-10. It is NOT the higgs-models naming convention because a certificate is bound to the exact directory that was rendered against, so a re-merge is a new directory and a new certificate. PREVIOUS wsl dir: owen_gp2w_prod. PREVIOUS darwin dir: owen_gp2w_prod. wsl: /home/telltale/higgs_v3_merged/owen_gp2w_prod is what the served arm renders; darwin: runtime/higgs-models/owen_gp2w_prod (under the Mac app-support dir) is what the MLX arm renders once the copy lands. The served numbers and the mlx block bind to THESE directories; a re-merge under the same name is a different model and re-measures.",
      "license": "boson-higgs-tts-3-research-noncommercial",
      "commercialUse": false,
      "sampleRate": 24000,
      "addedAt": "2026-09-07",
      "backends": {
        "served": {
          "maxChars": 800,
          "maxCharsSource": "catalog",
          "_maxCharsSourceNote": "'catalog' = declared here from the training data, not measured by a length sweep. OWEN'S RULE (2026-09-07): 'max chunk is what we trained on' -- the served max is the LONGEST TRAINING ROW of this directory's own corpus. owen_gp2w mix (build.log): 411 train rows, chars p50 793 / p75 1080 / p90 1407 / MAX 1687; seconds p50 49 / p90 87 / max 100; the recipe's served cap was 900 (160 rows, 38.9%, at or above it). Ladder evidence on this directory at 0.8/0.95/50: shape bank PS 0.261 [0.14,0.38] (11 early stops + 1 run-on of 46; failing classes micro/target/long/emdash/names, the same shapes as deathstalker); served-shape prose bank (Fuhrer-49, 50 renders, 26-1190 chars) PS 0.180 [0.083,0.285]. The narrator accepts only catalog | placeholder | length-sweep in maxCharsSource.",
          "safeMinChars": 600,
          "safeMaxChars": 800,
          "_safeBandNote": "SAFE BAND = the training corpus's INTERQUARTILE RANGE, measured 2026-09-09 (field notes 4n.37.20). owen_gp2w: p25 555, p75 1082 -> floor 600. Validated on thirdreich, whose IQR 532-985 predicted the measured best band 600-1000 (4/80 = 5.0% early stops [2.0-12.2], vs 18.8% below 600 and 20.3% above 1000). safeMaxChars is held at Owen's 800 ruling rather than the measured 1000; the measurement supports raising it. safeMinChars is the NEW half: before it, floor==cap==targetChars, so two 400-char paragraphs could not merge (800 > 700 cap) and a 400-char chunk shipped - which is how Owen's 2026-09-09 Mac render truncated at 323, 502 and 634 chars.",
          "_maxCharsNote": "800, OWEN'S RULING (2026-09-09): \"my models are limited to an absolute max of 800 characters before we start getting truncations\" - the cap is 800 on every fine-tune, both arms. PREVIOUS: 1687 = the training ceiling, see _maxCharsSourceNote. No length-sweep certificate exists for this directory; if one is run (the deathstalker 2026-09-05 sweep is the model: babble==0 across seeds AND min per-seed coverage >= 0.90, contiguous from the shortest tested length, scored by ASR alignment and never by duration ratio), record it here with maxCharsSource 'length-sweep'.",
          "edgeFadeMs": {
            "in": 10,
            "out": 25
          },
          "referenceSecondsCap": 30,
          "allowedControls": []
        },
        "mlx": {
          "_comment": "THE MAC'S OWN BLOCK FOR THIS FINE-TUNE, UNMEASURED: 900 mirrors deathstalker's MLX certificate (its sweep dropped text at 1200 where the served arm held), borrowed for the same architecture until this voice is swept on the Mac. targetChars cannot exceed the arm's maxChars (both sides refuse it), so the served target 1000 does not apply here until the MLX cap is measured above it.",
          "maxChars": 800,
          "maxCharsSource": "catalog",
          "safeMinChars": 600,
          "safeMaxChars": 800,
          "_safeBandNote": "SAFE BAND = the training corpus's INTERQUARTILE RANGE, measured 2026-09-09 (field notes 4n.37.20). owen_gp2w: p25 555, p75 1082 -> floor 600. Validated on thirdreich, whose IQR 532-985 predicted the measured best band 600-1000 (4/80 = 5.0% early stops [2.0-12.2], vs 18.8% below 600 and 20.3% above 1000). safeMaxChars is held at Owen's 800 ruling rather than the measured 1000; the measurement supports raising it. safeMinChars is the NEW half: before it, floor==cap==targetChars, so two 400-char paragraphs could not merge (800 > 700 cap) and a 400-char chunk shipped - which is how Owen's 2026-09-09 Mac render truncated at 323, 502 and 634 chars.",
          "_maxCharsNote": "800, OWEN'S RULING (2026-09-09): \"my models are limited to an absolute max of 800 characters before we start getting truncations\" - the cap is 800 on every fine-tune, both arms. PREVIOUS: 900, a PLACEHOLDER borrowed from deathstalker's MLX certificate; not measured on owen_gp2w_prod.",
          "edgeFadeMs": {
            "in": 10,
            "out": 25
          },
          "referenceSecondsCap": 30,
          "allowedControls": []
        }
      },
      "pace": {
        "median": 16.32,
        "mean": 15.98,
        "p05": 14.62,
        "p95": 16.84,
        "p99": 17.76,
        "n": 23,
        "method": "as deathstalker",
        "source": "served arm at temperature 0.8 on owen_gp2w_prod: night3/gp/para (Fuhrer-49 bank, 50 renders, 23 clean by the pace rule; pace_owen_whaa_served.json carries p1 14.27 / p5 14.62 / median 16.32 / mean 15.98 / p95 16.84 / p99 17.76, n_clean, clean_rule, band_rule). Seed band by BookForge's rule (2026-09-08): max = median x 1.2 = 19.58, min = median / 1.3 = 12.55 - a SEED whose RATIOS narrator re-centres on the book's own running median, not a fixed ceiling. One pace per voice, both arms.",
        "measuredOn": "2026-09-07"
      },
      "_paceNote": "THE VOICE'S MEASURED PACE, chars of text per second of audio, ONE PER VOICE for both arms (Owen 2026-09-06: 'it can be the same setting for both mac and windows since theyre the same voice and theyre pretty close'). From the training ladder's CLEAN renders (coverage >= 0.95, no early stop, no babble, 0.8 <= audio ratio <= 1.25, chars > 150). THE LENGTH GUARD'S SEED BAND IS DERIVED FROM THE MEDIAN AND THEN RE-CENTRED ON THE BOOK (2026-09-08): paceCharsPerSec = median = 16.32, maxCharsPerSec = median x 1.2 = 19.58 (above it a take is too SHORT - an early stop) and minCharsPerSec = median / 1.3 = 12.55 (below it a take RAN ON - a babble tail). ALL THREE ARE A SEED, NOT A CEILING: narrator keeps only the band's RATIOS and re-centres them on the running median of the book's own shipped takes once ten guarded chunks are in (truncation.PaceTracker), which is what actually answers Owen's worry - 'if its set to 17.11 chars/s, and one book averages 17.6/s, it shouldnt split and re-render everything over 17.11' - because a book that averages 17.6 BECOMES the reference and both edges move with it. The rule this replaces (the ladder's p99 x 1.15 and p05 / 1.15) could not do that, and MEASURED on the first full book rendered under it (Shift, mistborn, 1,313 chunks, 16.38 h, 2026-09-08) it was the wrong shape: the ladder bank is uniform nonfiction and Shift is fiction with dialogue, so the book's clean chunks paced 14.09 chars/s against the bank's 15.12 - the fixed long edge (11.97) fired on 340 healthy chunks at take 0 and the ladder re-rolled or split every one of them, while the short edge (19.27) sat at 1.37x the book's own pace and shipped 8 spot-aligned truncations (13-52 % of the words dropped, all at >= 1.23x the book's pace). The factors 1.2 and 1.3 are BookForge's PACE_GUARD_SHORT_FACTOR and PACE_GUARD_LONG_FACTOR. Files: E:\\\\training\\\\_campaigns\\\\2026-09-01-cod-full-rebuild\\\\higgs\\\\night3\\\\pace\\\\*.json. A voice with no pace block renders at narrator's default band (20 / 14.5, the Fuhrer whole-book measurement on deathstalker). NOTE: mistborn paces 11 % slower than deathstalker (p05 13.38 sits UNDER the 14.5 default long edge), which is why the band is per voice.",
      "_promotionNote": "PROMOTED 2026-09-10 11:20 to ow_v7_prod (ckpt-966, holdout 3.8899). THE RETRAIN THAT MADE THIS SHIPPABLE: the previous v6 model was unusable - it did not emit EOS and ran to the token cap on a large share of renders (mistborn 58%, deathstalker 29%) with a median 37.9 s of trailing silence, caused by RETAINED post-chunk pauses (slice_vtt --tail-s 4.0). Re-sliced at --tail-s 0.25 and retrained: 0/24 runaway; band NOT measured into config - its ladder gave 100-400 off a corpus with WHAA alignment drift (gate 7.4% vs GP 3.9%). Owen Morgan v7 was the positive control (0/24, same corpus, only the tail changed). Field notes 4n.53/4n.54. Merged dir sha256_16 9c68eb812f6c61d9. PREVIOUS ARM DIRS: wsl /home/telltale/higgs_v3_merged/ow_v7_prod, darwin runtime/higgs-models/ow_v7_prod. HuggingFace deliberately NOT updated - Owen has not given a green light.",
      "note": "The Owen Morgan narrator (God's People + Was Hitler an Atheist? sentences) as a merged Higgs v3 fine-tune -- the first Higgs Owen Morgan model; before it the only Higgs route for this voice was the zero-shot clip entry. Promoted 2026-09-07 on Owen's word to all three places: this PC (served arm), the Mac (MLX arm copy) and HuggingFace (source.ref).",
      "_kindNote": "kind 'checkpoint' — a MERGED fine-tune directory the server is started on, prompted TEXT-ONLY. No clips key: the voice is in the weights. It was called 'adapter' until 2026-09-04, which described the artifact we trained rather than the artifact that SERVES: vllm-omni has no runtime LoRA path, so the LoRA is an archival input to a merge and never a catalog field.",
      "chunkGap": {
        "injectS": 0.27,
        "targetJoinS": 0.53,
        "modelSelfTailS": 0.26,
        "readerSentenceGapS": 0.53,
        "modelInternalGapS": 0.5,
        "rule": "match-reader",
        "method": "silence = -40 dB relative to each clip's own peak, 20 ms hop. Reader gaps are measured AT VTT CUE BOUNDARIES with a 0.05 s floor (a boundary is known to be there, so a tight one is real); the model's own sentence gaps are inferred from silence length and so keep a 0.30 s floor, which is what separates a pause from a stop consonant. Tails are measured with no floor at all. Pauses over 2 s are paragraph and scene breaks and are excluded.",
        "source": "pause_match.py over the ow_v7 length ladder (runs/2026-09-10_0159_ow_v7_train_ladder/ow_v7_966, 200 of 512 renders) against the ow7_l training tier (200 clips). pause_verdict.json beside the run.",
        "measuredOn": "2026-09-11"
      },
      "_chunkGapNote": "THIS VOICE WAS THE ONE ACTUALLY SHIPPING A WRONG JOIN. Nothing set a gap per voice before 2026-09-11, so gaps.json carried classify_gap's blanket 0.6 s for every Higgs voice. On top of this model's own 0.26 s tail that made joins about 0.86 s, against a narrator who pauses 0.53 s between sentences - over 60% long, on every chunk boundary of every book.\n\nNow 0.26 + 0.27 = 0.53 s. RULE: match-reader. The model's own internal rhythm is 0.50 s, so rule C would give nearly the same answer (0.24) - the two agree here, which they do not on every voice.\n\nLONG PAUSES ARE NOT THE PROBLEM ON THIS VOICE, and that is worth recording because it is the clean control for the mistborn defect: over 2 s, the corpus carries 0.5 per 10k chars and the renders 0.4 - no amplification. mistborn is 3.3 -> 10.3 (3.1x) and deathstalker 2.1 -> 5.0 (2.3x). An assembly inject cannot touch that class of pause either way (field notes 4n.63).\n\nRE-MEASURE AFTER A RETRAIN: the tail moves with the corpus."
    },
    {
      "id": "thirdreich",
      "label": "Third Reich (Higgs v3 fine-tune)",
      "kind": "checkpoint",
      "_targetCharsNote": "THE CHUNK SIZE THE CODE PACKS TO, PER ARM (backends.served.targetChars / backends.mlx.targetChars), set by the trainer after training from the training clips' text lengths. Owen, 2026-09-05: 'maxChars is what the model was trained to do, and targetChars can be what the system actually uses. maxChars is informative, targetChars is used by the code directly.' The prep's cap AND merge floor: consecutive short paragraphs travel together until the next would overflow it, a paragraph already at it stands alone, walls stay walls. Never above the arm's maxChars - both sides refuse that by name. A fine-tune with no targetChars cannot be prepped. INTERIM VALUES (Owen, 2026-09-05 evening: 'set it to deathstalker's theoretical maximum of about 80 seconds'): each arm's certified maximum - served 1200 (~71 s at the measured 5.9 audio-s per 100 chars; the served sweep dropped text at 1500), MLX 900 (its sweep dropped text at 1200). MEASURED the same evening on both arms: this checkpoint ENDS ITSELF at ~1,980 frames (~79-82 s) whatever the server ceiling - a 2,173-char render on the Mac gave 79 s with the middle skipped (coverage 0.306) - so no target above ~1,350 chars can work on this checkpoint; the retrain that favours longer clips states its own numbers here, with the method in targetCharsSource.",
      "engineVersion": "v3",
      "voice": {
        "checkpoint": {
          "wsl": "/home/telltale/higgs_v3_merged/tr_v3_prod",
          "darwin": "runtime/higgs-models/tr_v3_prod"
        }
      },
      "_checkpointArmNote": "TWO ARMS, TWO COPIES. wsl: /home/telltale/higgs_v3_merged/tr_v3_prod -- the merged HF directory the training session wrote on the PC (tr_v3 train: 67.5 h paragraph runs + 38.2 h single sentences from the Evans trilogy, The Coming of the Third Reich / The Third Reich in Power / The Third Reich at War, 4937 rows; early stop at step 8316 (best holdout 3.4720 @7392); ladder pick ckpt-7084; 8.0 GB). darwin: the SAME directory rsynced to the Mac under BookForge's app-support runtime (~/Library/Application Support/BookForge/runtime/higgs-models/tr_v3_prod), sha-verified on the 2026-09-08 promotion. This is the FIRST Higgs fine-tune for this narrator; before it, zeroshot-thirdreich (base + one clip) was the only Higgs route.",
      "_checkpointDirNote": "wsl: /home/telltale/higgs_v3_merged/tr_v3_prod is what the served arm renders; darwin: runtime/higgs-models/tr_v3_prod is what the MLX arm renders. The served assertion and the mlx block bind to THESE directories; a re-merge under the same name is a different model and re-measures.",
      "license": "boson-higgs-tts-3-research-noncommercial",
      "commercialUse": false,
      "sampleRate": 24000,
      "addedAt": "2026-09-08",
      "backends": {
        "served": {
          "maxChars": 1000,
          "maxCharsSource": "catalog",
          "safeMinChars": 600,
          "safeMaxChars": 1000,
          "_safeBandNote": "MEASURED, and these fields ARE the measured band: safeMinChars 600, safeMaxChars 1000. runs/base_len swept 20 rungs x 16 renders on tr_v3_prod; the best contiguous band is 600-1000 at 4/80 early stops = 5.0% [2.0-12.2], against 18.8% below 600 and 20.3% above 1000. A full live book packed inside the band ran 4 failures in ~320 chunks = 1.25%. CORRECTED 2026-09-09: this note still claimed \"safeMaxChars is held at Owen's 800 ruling rather than the measured 1000\" AFTER commit 8d6082ed had already raised it to 1000 - the values moved and the note did not, so it contradicted the very field it documents. Its opening claim is RETIRED too: a safe band is NOT the corpus interquartile range. The IQR (tr_v3 p25 532 / p75 985) happened to predict this one, but a corpus-derived band was wrong in production within a day; bands come from a length sweep only (field notes 4n.37.19-21, 4n.47). safeMinChars remains the half that matters most: before it, floor==cap==targetChars, so two 400-char paragraphs could not merge and a 400-char chunk shipped - which is how Owen's 2026-09-09 Mac render truncated at 323, 502 and 634 chars. PREVIOUS NOTE: SAFE BAND = the training corpus's INTERQUARTILE RANGE, measured 2026-09-09 (field notes 4n.37.20). tr_v3: p25 532, p75 985 -> floor 600. Validated on thirdreich, whose IQR 532-985 predicted the measured best band 600-1000 (4/80 = 5.0% early stops [2.0-12.2], vs 18.8% below 600 and 20.3% above 1000). safeMaxChars is held at Owen's 800 ruling rather than the measured 1000; the measurement supports raising it. safeMinChars is the NEW half: before it, floor==cap==targetChars, so two 400-char paragraphs could not merge (800 > 700 cap) and a 400-char chunk shipped - which is how Owen's 2026-09-09 Mac render truncated at 323, 502 and 634 chars.",
          "_maxCharsNote": "MEASURED 2026-09-09, raising Owen's earlier 800 ruling: runs/base_len swept 20 rungs x 16 renders on tr_v3_prod and the best contiguous band is 600-1000 (4/80 early stops = 5.0% [2.0-12.2]); 900 and 1000 measure 6.2% each, indistinguishable from 700's 12.5%, while 1100+ climbs to 20.3%. A full live book packed inside the band ran 4 failures in ~320 chunks = 1.25%. Owen: \"you said third reich is capable of going up to 1000? then go ahead and set the upper limit to 1000.\" PREVIOUS: 800, Owen's ruling of the same morning, made before the sweep. 800, OWEN'S RULING (2026-09-09): \"my models are limited to an absolute max of 800 characters before we start getting truncations\" - the cap is 800 on every fine-tune, both arms. PREVIOUS: 1623, the LONGEST TRAINING ROW of tr_v3_prod's corpus (Owen's rule: max chunk is what the model trained on); no length sweep yet.",
          "edgeFadeMs": {
            "in": 10,
            "out": 25
          },
          "referenceSecondsCap": 30,
          "allowedControls": [],
          "_maxCharsSourceNote": "'catalog' = declared here from the training data, not measured by a length sweep. tr_v3 mix (build.log): 4937 train rows, chars p50 732 / p75 985 / p90 1263 / MAX 1623; seconds p50 48 / p90 83 / max 100; served cap 1000 non-strict (1183 rows, 24.0%, at or above it). Corpus: the Evans trilogy, all clean hours, front matter / back matter / chapter transitions cut by raw-audio cut points; gate drops 0.6% long, 2.9% short. Ladder evidence at 0.8/0.95/50: see _promotionNote."
        },
        "mlx": {
          "_comment": "THE MAC'S OWN BLOCK FOR THIS FINE-TUNE, UNMEASURED on tr_v3_prod: 900 mirrors deathstalker's MLX certificate as the conservative number for the same architecture; `placeholder` says nobody has measured THIS voice on this arm. Replace both numbers and the source after a Mac sweep.",
          "maxChars": 1000,
          "maxCharsSource": "catalog",
          "safeMinChars": 600,
          "safeMaxChars": 1000,
          "_safeBandNote": "MEASURED, and these fields ARE the measured band: safeMinChars 600, safeMaxChars 1000. runs/base_len swept 20 rungs x 16 renders on tr_v3_prod; the best contiguous band is 600-1000 at 4/80 early stops = 5.0% [2.0-12.2], against 18.8% below 600 and 20.3% above 1000. A full live book packed inside the band ran 4 failures in ~320 chunks = 1.25%. CORRECTED 2026-09-09: this note still claimed \"safeMaxChars is held at Owen's 800 ruling rather than the measured 1000\" AFTER commit 8d6082ed had already raised it to 1000 - the values moved and the note did not, so it contradicted the very field it documents. Its opening claim is RETIRED too: a safe band is NOT the corpus interquartile range. The IQR (tr_v3 p25 532 / p75 985) happened to predict this one, but a corpus-derived band was wrong in production within a day; bands come from a length sweep only (field notes 4n.37.19-21, 4n.47). safeMinChars remains the half that matters most: before it, floor==cap==targetChars, so two 400-char paragraphs could not merge and a 400-char chunk shipped - which is how Owen's 2026-09-09 Mac render truncated at 323, 502 and 634 chars. PREVIOUS NOTE: SAFE BAND = the training corpus's INTERQUARTILE RANGE, measured 2026-09-09 (field notes 4n.37.20). tr_v3: p25 532, p75 985 -> floor 600. Validated on thirdreich, whose IQR 532-985 predicted the measured best band 600-1000 (4/80 = 5.0% early stops [2.0-12.2], vs 18.8% below 600 and 20.3% above 1000). safeMaxChars is held at Owen's 800 ruling rather than the measured 1000; the measurement supports raising it. safeMinChars is the NEW half: before it, floor==cap==targetChars, so two 400-char paragraphs could not merge (800 > 700 cap) and a 400-char chunk shipped - which is how Owen's 2026-09-09 Mac render truncated at 323, 502 and 634 chars.",
          "_maxCharsNote": "MEASURED 2026-09-09, raising Owen's earlier 800 ruling: runs/base_len swept 20 rungs x 16 renders on tr_v3_prod and the best contiguous band is 600-1000 (4/80 early stops = 5.0% [2.0-12.2]); 900 and 1000 measure 6.2% each, indistinguishable from 700's 12.5%, while 1100+ climbs to 20.3%. A full live book packed inside the band ran 4 failures in ~320 chunks = 1.25%. Owen: \"you said third reich is capable of going up to 1000? then go ahead and set the upper limit to 1000.\" PREVIOUS: 800, Owen's ruling of the same morning, made before the sweep. 800, OWEN'S RULING (2026-09-09): \"my models are limited to an absolute max of 800 characters before we start getting truncations\" - the cap is 800 on every fine-tune, both arms. PREVIOUS: 900, a PLACEHOLDER borrowed from deathstalker's MLX certificate; not measured on tr_v3_prod.",
          "edgeFadeMs": {
            "in": 10,
            "out": 25
          },
          "referenceSecondsCap": 30,
          "allowedControls": []
        }
      },
      "pace": {
        "median": 15.29,
        "mean": 15.29,
        "p05": 14.24,
        "p95": 16.12,
        "p99": 16.57,
        "n": 37,
        "method": "as deathstalker",
        "measuredOn": "2026-09-08",
        "source": "served arm at 0.8/0.95/50: night3/tr3/para (Fuhrer-49 prose bank, 50 renders, 37 clean) -> pace_thirdreich_served.json (higgs_shape_ladder verdict --pace-json): p1 13.82 / p5 14.24 / median 15.29 / mean 15.29 / p95 16.12 / p99 16.57, clean_rule and band_rule inside. FIRST pace measurement for this voice on Higgs."
      },
      "_paceNote": "THE VOICE'S MEASURED PACE, chars of text per second of audio, ONE PER VOICE for both arms (Owen 2026-09-06: 'it can be the same setting for both mac and windows since theyre the same voice and theyre pretty close'). From the training ladder's CLEAN renders (coverage >= 0.95, no early stop, no babble, 0.8 <= audio ratio <= 1.25, chars > 150). THE LENGTH GUARD'S SEED BAND IS DERIVED FROM THE MEDIAN AND THEN RE-CENTRED ON THE BOOK (2026-09-08): paceCharsPerSec = median = 15.29, maxCharsPerSec = median x 1.2 = 18.35 (above it a take is too SHORT - an early stop) and minCharsPerSec = median / 1.3 = 11.76 (below it a take RAN ON - a babble tail). ALL THREE ARE A SEED, NOT A CEILING: narrator keeps only the band's RATIOS and re-centres them on the running median of the book's own shipped takes once ten guarded chunks are in (truncation.PaceTracker), which is what actually answers Owen's worry - 'if its set to 17.11 chars/s, and one book averages 17.6/s, it shouldnt split and re-render everything over 17.11' - because a book that averages 17.6 BECOMES the reference and both edges move with it. The rule this replaces (the ladder's p99 x 1.15 and p05 / 1.15) could not do that, and MEASURED on the first full book rendered under it (Shift, mistborn, 1,313 chunks, 16.38 h, 2026-09-08) it was the wrong shape: the ladder bank is uniform nonfiction and Shift is fiction with dialogue, so the book's clean chunks paced 14.09 chars/s against the bank's 15.12 - the fixed long edge (11.97) fired on 340 healthy chunks at take 0 and the ladder re-rolled or split every one of them, while the short edge (19.27) sat at 1.37x the book's own pace and shipped 8 spot-aligned truncations (13-52 % of the words dropped, all at >= 1.23x the book's pace). The factors 1.2 and 1.3 are BookForge's PACE_GUARD_SHORT_FACTOR and PACE_GUARD_LONG_FACTOR. Files: E:\\\\training\\\\_campaigns\\\\2026-09-01-cod-full-rebuild\\\\higgs\\\\night3\\\\pace\\\\*.json. A voice with no pace block renders at narrator's default band (20 / 14.5, the Fuhrer whole-book measurement on deathstalker). NOTE: mistborn paces 11 % slower than deathstalker (p05 13.38 sits UNDER the 14.5 default long edge), which is why the band is per voice.",
      "note": "The Third Reich narrator (Sean Pratt, the Evans trilogy) as a merged Higgs v3 fine-tune, tr_v3 ckpt-7084. The largest corpus of the v3 campaign: 105.7 h gated (67.5 h paragraph runs + 38.2 h sentences) against mistborn's 30.3 h and deathstalker's 19.5 h; trained to epoch 6.7 of 12, best holdout 3.4720, the lowest of the campaign. Text through the same model number pass BookForge runs at render.",
      "_kindNote": "kind 'checkpoint' — a MERGED fine-tune directory the server is started on, prompted TEXT-ONLY. No clips key: the voice is in the weights. It was called 'adapter' until 2026-09-04, which described the artifact we trained rather than the artifact that SERVES: vllm-omni has no runtime LoRA path, so the LoRA is an archival input to a merge and never a catalog field.",
      "source": {
        "type": "hf",
        "ref": "owenmorgan/thirdreich-higgs-v3"
      },
      "_sourceNote": "WHERE A MACHINE DOWNLOADS THIS CHECKPOINT FROM (Settings -> Higgs -> Download): a PRIVATE HuggingFace repo under Owen's account, uploaded from the WSL directory on the 2026-09-08 promotion (see _promotionNote for the sha256 a downloaded copy must match).",
      "_promotionNote": "2026-09-08 (Owen 2026-09-07 23:0x 'start training TR when it's ready' + 2026-09-08 'remember to record the cps/max chunk size/etc as usual in the model config'). Ladder over the 4 lowest-holdout checkpoints of tr_v3: pick ckpt-7084. Record: night3/tr3/ (pick_ckpt.log, shape/, para/, sent/, pace_thirdreich_served.json, train.log, build_v3b.log). model.safetensors sha256 d0aa38f6447ccdbb3177615237c177c6ffb4e2462b7d71691f39ac098befc54f (merge_sha256.txt). Installed: WSL /home/telltale/higgs_v3_merged/tr_v3_prod; Mac runtime/higgs-models/tr_v3_prod (rsync, sha-verified); HF owenmorgan/thirdreich-higgs-v3 (private)."
    },
    {
      "id": "sigma",
      "label": "Sigma (Higgs v3 fine-tune)",
      "kind": "checkpoint",
      "engineVersion": "v3",
      "voice": {
        "checkpoint": {
          "wsl": "/home/telltale/higgs_v3_merged/sigma_prod",
          "darwin": "runtime/higgs-models/sigma_prod"
        }
      },
      "source": {
        "type": "hf",
        "ref": "owenmorgan/sigma-higgs-v3"
      },
      "license": "boson-higgs-tts-3-research-noncommercial",
      "commercialUse": false,
      "sampleRate": 24000,
      "addedAt": "2026-09-11",
      "backends": {
        "served": {
          "maxChars": 1100,
          "maxCharsSource": "length-sweep",
          "edgeFadeMs": {
            "in": 10,
            "out": 25
          },
          "referenceSecondsCap": 30,
          "allowedControls": [],
          "safeMinChars": 500,
          "safeMaxChars": 1100,
          "_safeBandNote": "MEASURED 2026-09-11 00:32 by the length ladder (rungs 100-1600, 8 prompts x 4 seeds): band 500-1100, in-band 0/160 = 0.0% (95% hi 2.3%). Winner sigma_1426 -- band winner: fewest in-band failures, margin 0.0 renders.",
          "_maxCharsNote": "1100, RAISED 2026-09-11 00:32 so the cap is not below the measured band (a max under the band is refused by name at load). PREVIOUS: 800."
        },
        "mlx": {
          "maxChars": 1100,
          "maxCharsSource": "length-sweep",
          "edgeFadeMs": {
            "in": 10,
            "out": 25
          },
          "referenceSecondsCap": 30,
          "allowedControls": [],
          "_comment": "Same band and cap as the served arm. One pace per voice, both arms (Owen 2026-09-06): the Mac and the PC serve the same weights, so a per-arm pace would be two names for one number.",
          "safeMinChars": 500,
          "safeMaxChars": 1100,
          "_safeBandNote": "MEASURED 2026-09-11 00:32 by the length ladder (rungs 100-1600, 8 prompts x 4 seeds): band 500-1100, in-band 0/160 = 0.0% (95% hi 2.3%). Winner sigma_1426 -- band winner: fewest in-band failures, margin 0.0 renders.",
          "_maxCharsNote": "1100, RAISED 2026-09-11 00:32 so the cap is not below the measured band (a max under the band is refused by name at load). PREVIOUS: 800."
        }
      },
      "note": "THE FIRST VOICE TRAINED ON RVC-CONVERTED AUDIO, and it exists because of an experiment rather than a book. The corpus is deathstalker's own build7 clips (998 train rows, the SAME rows json and cap as ds_v7) converted clip-by-clip through the Sigma Male Narrator RVC model, downsampled 48k->24k by exact 2:1 polyphase decimation, and trained with only --build-root changed. That makes sigma and ds_v7 a controlled A/B whose single variable is RVC (HIGGS_FIELD_NOTES 4n.58-4n.60).\n\nCROSS-VOICE, SO THERE IS NO GROUND TRUTH. RVC did not clean a recording here - it resynthesized a voice that never existed from deathstalker's content features. The artifacts ARE the voice, which is why no speaker-similarity check applies and Owen's ear is the whole of the quality verdict (2026-09-11: \"the render sounds great. came out well\").\n\nWHY ITS NUMBERS LOOK BETTER THAN DEATHSTALKER'S, AND WHY THAT IS NOT A COMPLIMENT: holdout 3.2513 against ds_v7's 3.8353 on identical rows, and ds_v7 EARLY-STOPPED at step 930 while sigma was still improving at 1426. Cross-entropy on audio codes falls mechanically when the target is more predictable, and RVC output is exactly that - a resynthesis that discards room tone, breath variation and level drift. The ladder rewards the same smoothness. So the lower loss and the wider band mean 'easier to model', NOT 'better voice'. Do not carry this comparison over to a same-voice cleanup, where a real narrator exists to deviate from.",
      "pace": {
        "median": 15.56,
        "mean": 15.6,
        "p05": 14.18,
        "p95": 17.23,
        "p99": 18.17,
        "n": 422,
        "method": "chars = len(bank text as rendered); seconds = full wav duration as written by the engine. Per-render chars/audio_s, then the percentiles of that.",
        "source": "served arm, sigma full ladder night4/runs/sigma_ladder/ds_sigma_prod (bank_ds_len, 512 renders, 4 seeds, T 0.8/0.95/50)",
        "measuredOn": "2026-09-11"
      },
      "_paceNote": "MEASURED 2026-09-11 from the sigma length ladder's CLEAN renders by the same rule every other pace block uses (clean renders: ok, coverage >= 0.95, no early stop, no babble, 0.80 <= audio_ratio <= 1.25, audio_s > 2, chars > 150): 512 renders -> 422 clean. BookForge derives the truncation guard from the median (higgsLengthBand): paceCharsPerSec 15.56, maxCharsPerSec 20.23 (above it a take stopped SHORT), minCharsPerSec 11.97 (below it it RAN ON). All three are a SEED - narrator keeps only the ratios and re-centres them on the book's own running median once ten guarded takes are in (truncation.PaceTracker). Tool: pipeline/untreated/measure_pace.py. File: night3/pace/sigma_served.json. RE-MEASURE AFTER ANY RETRAIN.",
      "_promotionNote": "PROMOTED 2026-09-11 00:32. Winner sigma_1426 (band winner: fewest in-band failures, margin 0.0 renders). Band 500-1100 from the length ladder: in-band 0/160 = 0.0% (95% hi 2.3%). Merged dir /home/telltale/higgs_v3_merged/sigma_prod, sha256 32d108ae3c22572ba42ee588649872312dede93e4655992ed0fbcb2853addf02. maxCharsSource=length-sweep. HuggingFace UPDATED: owenmorgan/sigma-higgs-v3, private, 12 files, uploaded 2026-09-11 on Owen's explicit green light. promote_voice.py does not do this step and its stock note says the opposite; corrected here by hand.",
      "_checkpointDirNote": "THE WEIGHTS THIS VOICE RENDERS, per arm, named so a reader can check the note against the row (that is what tools/test-higgs-engine.js enforces here).\n  served / wsl:  /home/telltale/higgs_v3_merged/sigma_prod\n  mlx / darwin:  runtime/higgs-models/sigma_prod\nBoth are THE SAME merged directory - the WSL copy rsynced to the Mac and sha256-verified per weight file. Merged from ckpt-1426 of night2/runs/ds_sigma_full (holdout 3.25130, the lowest of the run; the run was stopped by the operator at ~step 1475 of 2994 while still improving, so the checkpoint is a minimum of what was measured, not of what was reachable). merge sha256 32d108ae3c22572b. The cap and band below were measured against THESE weights and are not transferable to a retrain.",
      "_checkpointArmNote": "TWO ARMS, TWO COPIES, one basename: sigma_prod. wsl /home/telltale/higgs_v3_merged/sigma_prod, darwin runtime/higgs-models/sigma_prod. Promoted 2026-09-11, the first promotion of a voice trained on RVC-converted audio. Staged as sigma_1426 until promotion and renamed by promote_voice.py, which is the convention - the pre-promotion name carries the checkpoint step so the ladder verdict and the directory cannot drift apart.",
      "chunkGap": {
        "injectS": 0.62,
        "targetJoinS": 0.84,
        "modelSelfTailS": 0.22,
        "readerSentenceGapS": 0.84,
        "modelInternalGapS": 0.66,
        "rule": "match-reader",
        "method": "silence = -40 dB relative to each clip's own peak, 20 ms hop, runs >= 0.30 s; sentence boundaries only (pauses over 2 s are paragraph/scene breaks and are excluded). Tails measured with NO minimum length. RELATIVE, not absolute, because RVC output and a fine-tune's renders are both louder than the corpus and an absolute floor would report a level difference as a pause difference.",
        "source": "pause_match.py over the sigma ladder run (night4/runs/sigma_ladder/ds_sigma_prod, 200 renders) against the deathstalker build7 long tier (200 clips). pause_verdict_sigma.json.",
        "measuredOn": "2026-09-11"
      },
      "_chunkGapNote": "THE INJECT, NET OF THE MODEL'S OWN TAIL. A join is (modelSelfTailS + injectS) = 0.22 + 0.62 = 0.84 s. Owen 2026-09-10, settling whether the tail survives into assembly: \"narrator doesnt trim it. the silence of 0.22 is effectively zero. it needs padding.\" So the tail is real and is SUBTRACTED; \"effectively zero\" is about perception, not arithmetic - 0.22 s does not work as a join on its own.\n\nRULE: match-reader. The narrator pauses 0.84 s between sentences in the training clips and the model pauses 0.66 s inside one render, so the model UNDER-pauses (ratio 0.79). Two other targets were computed and rejected: matching the model's own rhythm (join 0.66) and the thirdreich-era 0.75x calibration (join 0.71, which lands BELOW the model's internal gap and would make chunk joins tighter than sentence boundaries inside a chunk - the collision Owen caught by ear in The Mysterious Stranger).\n\nWHAT THIS REPLACES: nothing was set per voice before, so gaps.json carried classify_gap's hardcoded 0.6 s default for EVERY Higgs voice. 0.62 is therefore a 0.02 s change for sigma - the old default happened to be nearly right for this voice, and is not known to be right for any other. RE-MEASURE PER VOICE, and after any retrain: tail geometry moves with the corpus."
    },
    {
      "id": "zeroshot-thirdreich",
      "label": "Zero-shot · Third Reich (reference clip on base Higgs v3)",
      "kind": "clips",
      "engineVersion": "v3",
      "voice": {
        "clips": [
          {
            "path": "thirdreich.wav",
            "transcript": "Almost every aspect of political, social and associational life was affected, at every level from the nation to the village. The Nazi takeover of the federated states provided a key component in this process.",
            "seconds": 15,
            "_source": "E:\\training\\thirdreich\\build\\tr_6h1\\wavs\\thirdreich_00002337.wav (training corpus, read-only; copied into the PC's runtime/higgs-models/refs 2026-09-06)"
          }
        ]
      },
      "license": "boson-higgs-tts-3-research-noncommercial",
      "commercialUse": false,
      "sampleRate": 24000,
      "addedAt": "2026-09-06",
      "_zeroShotNote": "ZERO-SHOT: the BASE Higgs v3 weights (bosonai/higgs-audio-v3-tts-4b, the HF-cache snapshot the launcher falls back to when HIGGS_MODEL_DIR is unset - the same server a `default` voice runs on) conditioned by ONE reference clip in every request. Asked for by Owen 2026-09-06 ('give me a zero shot option on the higgs/narration modal'). THE CLIP lives PERMANENTLY in the models area - <userData>/runtime/higgs-models/refs/<path> on every machine (Windows: %APPDATA%\\BookForge\\runtime\\higgs-models\\refs; Mac: ~/Library/Application Support/BookForge/runtime/higgs-models/refs), beside runtime/higgs-models/base and next to runtime/orpheus-models; a bare file name here resolves against that folder (higgsReferenceClipPath) and is refused by name if the file is not there. Owen, 2026-09-06: 'ref clips can be saved permanently in the same area where models are saved.' NOT in the repo - it is a voice artifact staged per machine like a checkpoint. Chosen by the training session from its TREATED corpus (denoised, bed-normalized: the clone copies whatever noise the clip carries), one clean ~15 s clip rather than two joined; its transcript is the corpus row's book-exact text (no digits, no quotes). Training's length guidance: 12-16 s (HIGH that it works - a 13.2 s clip drove a whole 37-chunk subset on SGLang; MEDIUM that it is optimal, length not swept). COST: ~25 placeholder positions per second of reference plus the transcript, charged to SGLang-Omni's hard 4096 context by narrator's sgl_served.reference_token_bound; at 600 chars the frame cap keeps its full 2.0x ceiling. HOW IT RIDES: on SGLang-Omni as `references: [{data: <base64 wav>, media_type, text}]` - the SpeechReference `data` branch, which never touches the server's filesystem (no --allowed-local-media-path); on vllm-omni as one data: URI; on the Mac's MLX arm encoded once at load. SAMPLING on the served arm is narrator's SERVER_DEFAULT_SAMPLING (T 1.0 / top_p 0.95 / top_k 50 - the numbers every training run used; the base snapshot ships NO generation_config.json, so there is nothing to read); the `sampling` block below is a MIRROR of that for the reader, not a source. Training's LOW-confidence preference for T 0.8 on base is recorded here and not applied - unmeasured. EXPECT the fine-tunes' ~1-in-10 retake rate or worse (run-ons and early stops are stochastic per render); chunk-to-chunk identity drift is UNMEASURED for zero-shot. Identity of a clone against the narrator: ~0.68 cosine one clip vs 0.766 self-ceiling (see _referenceComment), i.e. 92 % of the fine-tune's identity and none of its phrasing.",
      "_relationNote": "the `thirdreich` fine-tune (tr_v3_prod, 2026-09-08) is this narrator's trained Higgs route; this zero-shot entry stays as the base-weights comparison and the fallback when the checkpoint has not landed on a machine.",
      "backends": {
        "served": {
          "maxChars": 600,
          "maxCharsSource": "placeholder",
          "targetChars": 600,
          "targetCharsSource": "the zero-shot wall (Owen 2026-09-06, 'get it working'): <= 600 chars keeps its text, 900 drops the tail reproducibly, cloned or not - HIGH on vllm-omni where it was measured, MEDIUM on SGLang (training has not re-measured base + clip there). See the `default` entry's _maxCharsNote for the measurement.",
          "edgeFadeMs": {
            "in": 10,
            "out": 25
          },
          "referenceSecondsCap": 30,
          "allowedControls": []
        },
        "mlx": {
          "maxChars": 600,
          "maxCharsSource": "placeholder",
          "targetChars": 600,
          "targetCharsSource": "the zero-shot placeholder, UNMEASURED on the MLX arm for this clip (the same placeholder the `default` entry carries there)",
          "edgeFadeMs": {
            "in": 10,
            "out": 25
          },
          "referenceSecondsCap": 30,
          "allowedControls": []
        }
      }
    },
    {
      "id": "zeroshot-owen-morgan",
      "label": "Zero-shot · Owen Morgan (reference clip on base Higgs v3)",
      "kind": "clips",
      "engineVersion": "v3",
      "voice": {
        "clips": [
          {
            "path": "owen-morgan.wav",
            "transcript": "He had no career, no diploma, no friends, and no plan. The war gave him all of those things at once - a cause, a community, a structure, and a role. The regiment became his home, and it would remain the only real home he ever had.",
            "seconds": 15.12,
            "_source": "E:\\training\\owen\\build\\owen_sel2h\\wavs\\owen_00000241.wav (training corpus, read-only; copied into the PC's runtime/higgs-models/refs 2026-09-06)"
          }
        ]
      },
      "license": "boson-higgs-tts-3-research-noncommercial",
      "commercialUse": false,
      "sampleRate": 24000,
      "addedAt": "2026-09-06",
      "_zeroShotNote": "ZERO-SHOT: the BASE Higgs v3 weights (bosonai/higgs-audio-v3-tts-4b, the HF-cache snapshot the launcher falls back to when HIGGS_MODEL_DIR is unset - the same server a `default` voice runs on) conditioned by ONE reference clip in every request. Asked for by Owen 2026-09-06 ('give me a zero shot option on the higgs/narration modal'). THE CLIP lives PERMANENTLY in the models area - <userData>/runtime/higgs-models/refs/<path> on every machine (Windows: %APPDATA%\\BookForge\\runtime\\higgs-models\\refs; Mac: ~/Library/Application Support/BookForge/runtime/higgs-models/refs), beside runtime/higgs-models/base and next to runtime/orpheus-models; a bare file name here resolves against that folder (higgsReferenceClipPath) and is refused by name if the file is not there. Owen, 2026-09-06: 'ref clips can be saved permanently in the same area where models are saved.' NOT in the repo - it is a voice artifact staged per machine like a checkpoint. Chosen by the training session from its TREATED corpus (denoised, bed-normalized: the clone copies whatever noise the clip carries), one clean ~15 s clip rather than two joined; its transcript is the corpus row's book-exact text (no digits, no quotes). Training's length guidance: 12-16 s (HIGH that it works - a 13.2 s clip drove a whole 37-chunk subset on SGLang; MEDIUM that it is optimal, length not swept). COST: ~25 placeholder positions per second of reference plus the transcript, charged to SGLang-Omni's hard 4096 context by narrator's sgl_served.reference_token_bound; at 600 chars the frame cap keeps its full 2.0x ceiling. HOW IT RIDES: on SGLang-Omni as `references: [{data: <base64 wav>, media_type, text}]` - the SpeechReference `data` branch, which never touches the server's filesystem (no --allowed-local-media-path); on vllm-omni as one data: URI; on the Mac's MLX arm encoded once at load. SAMPLING on the served arm is narrator's SERVER_DEFAULT_SAMPLING (T 1.0 / top_p 0.95 / top_k 50 - the numbers every training run used; the base snapshot ships NO generation_config.json, so there is nothing to read); the `sampling` block below is a MIRROR of that for the reader, not a source. Training's LOW-confidence preference for T 0.8 on base is recorded here and not applied - unmeasured. EXPECT the fine-tunes' ~1-in-10 retake rate or worse (run-ons and early stops are stochastic per render); chunk-to-chunk identity drift is UNMEASURED for zero-shot. Identity of a clone against the narrator: ~0.68 cosine one clip vs 0.766 self-ceiling (see _referenceComment), i.e. 92 % of the fine-tune's identity and none of its phrasing.",
      "_relationNote": "a DIFFERENT PRODUCT from the `owen` fine-tune (promoted 2026-09-07), not a second route to it: base weights + this clip. Until 2026-09-07 this read 'no Higgs fine-tune exists for this voice' and was the only Higgs route.",
      "backends": {
        "served": {
          "maxChars": 600,
          "maxCharsSource": "placeholder",
          "targetChars": 600,
          "targetCharsSource": "the zero-shot wall (Owen 2026-09-06, 'get it working'): <= 600 chars keeps its text, 900 drops the tail reproducibly, cloned or not - HIGH on vllm-omni where it was measured, MEDIUM on SGLang (training has not re-measured base + clip there). See the `default` entry's _maxCharsNote for the measurement.",
          "edgeFadeMs": {
            "in": 10,
            "out": 25
          },
          "referenceSecondsCap": 30,
          "allowedControls": []
        },
        "mlx": {
          "maxChars": 600,
          "maxCharsSource": "placeholder",
          "targetChars": 600,
          "targetCharsSource": "the zero-shot placeholder, UNMEASURED on the MLX arm for this clip (the same placeholder the `default` entry carries there)",
          "edgeFadeMs": {
            "in": 10,
            "out": 25
          },
          "referenceSecondsCap": 30,
          "allowedControls": []
        }
      }
    },
    {
      "id": "zeroshot-deathstalker",
      "label": "Zero-shot · Deathstalker (reference clip on base Higgs v3)",
      "kind": "clips",
      "engineVersion": "v3",
      "voice": {
        "clips": [
          {
            "path": "deathstalker.wav",
            "transcript": "Freedom from anxiety is characterized by three inner attitudes. If what we have we receive as a gift, and if what we have is to be cared for by God, and if what we have is available to others, then we will possess freedom from anxiety.",
            "seconds": 15.09,
            "_source": "E:\\training\\deathstalker\\build\\ds_ad5s\\wavs\\cd_cd_00000918.wav (training corpus, read-only; copied into the PC's runtime/higgs-models/refs 2026-09-06)"
          }
        ]
      },
      "license": "boson-higgs-tts-3-research-noncommercial",
      "commercialUse": false,
      "sampleRate": 24000,
      "addedAt": "2026-09-06",
      "_zeroShotNote": "ZERO-SHOT: the BASE Higgs v3 weights (bosonai/higgs-audio-v3-tts-4b, the HF-cache snapshot the launcher falls back to when HIGGS_MODEL_DIR is unset - the same server a `default` voice runs on) conditioned by ONE reference clip in every request. Asked for by Owen 2026-09-06 ('give me a zero shot option on the higgs/narration modal'). THE CLIP lives PERMANENTLY in the models area - <userData>/runtime/higgs-models/refs/<path> on every machine (Windows: %APPDATA%\\BookForge\\runtime\\higgs-models\\refs; Mac: ~/Library/Application Support/BookForge/runtime/higgs-models/refs), beside runtime/higgs-models/base and next to runtime/orpheus-models; a bare file name here resolves against that folder (higgsReferenceClipPath) and is refused by name if the file is not there. Owen, 2026-09-06: 'ref clips can be saved permanently in the same area where models are saved.' NOT in the repo - it is a voice artifact staged per machine like a checkpoint. Chosen by the training session from its TREATED corpus (denoised, bed-normalized: the clone copies whatever noise the clip carries), one clean ~15 s clip rather than two joined; its transcript is the corpus row's book-exact text (no digits, no quotes). Training's length guidance: 12-16 s (HIGH that it works - a 13.2 s clip drove a whole 37-chunk subset on SGLang; MEDIUM that it is optimal, length not swept). COST: ~25 placeholder positions per second of reference plus the transcript, charged to SGLang-Omni's hard 4096 context by narrator's sgl_served.reference_token_bound; at 600 chars the frame cap keeps its full 2.0x ceiling. HOW IT RIDES: on SGLang-Omni as `references: [{data: <base64 wav>, media_type, text}]` - the SpeechReference `data` branch, which never touches the server's filesystem (no --allowed-local-media-path); on vllm-omni as one data: URI; on the Mac's MLX arm encoded once at load. SAMPLING on the served arm is narrator's SERVER_DEFAULT_SAMPLING (T 1.0 / top_p 0.95 / top_k 50 - the numbers every training run used; the base snapshot ships NO generation_config.json, so there is nothing to read); the `sampling` block below is a MIRROR of that for the reader, not a source. Training's LOW-confidence preference for T 0.8 on base is recorded here and not applied - unmeasured. EXPECT the fine-tunes' ~1-in-10 retake rate or worse (run-ons and early stops are stochastic per render); chunk-to-chunk identity drift is UNMEASURED for zero-shot. Identity of a clone against the narrator: ~0.68 cosine one clip vs 0.766 self-ceiling (see _referenceComment), i.e. 92 % of the fine-tune's identity and none of its phrasing.",
      "_relationNote": "a DIFFERENT PRODUCT from the `deathstalker` fine-tune, not a second route to it: base weights + this clip. The training session measured that adding a clip to the plain-trained fine-tune buys no identity (ECAPA 0.86 with and without, HIGGS_FIELD_NOTES 4n.10), so the fine-tune stays text-only and this entry is the clone",
      "backends": {
        "served": {
          "maxChars": 600,
          "maxCharsSource": "placeholder",
          "targetChars": 600,
          "targetCharsSource": "the zero-shot wall (Owen 2026-09-06, 'get it working'): <= 600 chars keeps its text, 900 drops the tail reproducibly, cloned or not - HIGH on vllm-omni where it was measured, MEDIUM on SGLang (training has not re-measured base + clip there). See the `default` entry's _maxCharsNote for the measurement.",
          "edgeFadeMs": {
            "in": 10,
            "out": 25
          },
          "referenceSecondsCap": 30,
          "allowedControls": []
        },
        "mlx": {
          "maxChars": 600,
          "maxCharsSource": "placeholder",
          "targetChars": 600,
          "targetCharsSource": "the zero-shot placeholder, UNMEASURED on the MLX arm for this clip (the same placeholder the `default` entry carries there)",
          "edgeFadeMs": {
            "in": 10,
            "out": 25
          },
          "referenceSecondsCap": 30,
          "allowedControls": []
        }
      }
    },
    {
      "id": "zeroshot-mistborn",
      "label": "Zero-shot · Mistborn (reference clip on base Higgs v3)",
      "kind": "clips",
      "engineVersion": "v3",
      "voice": {
        "clips": [
          {
            "path": "mistborn.wav",
            "transcript": "That he should be the one who finally discovered the great Hero of Terris prophecy is a matter that would cause me to laugh, had events turned out just a little differently.",
            "seconds": 14.87,
            "_source": "E:\\training\\mistborn\\build\\mb_h1s\\wavs\\mb_mb_00000472.wav (training corpus, read-only; copied into the PC's runtime/higgs-models/refs 2026-09-06)"
          }
        ]
      },
      "license": "boson-higgs-tts-3-research-noncommercial",
      "commercialUse": false,
      "sampleRate": 24000,
      "addedAt": "2026-09-06",
      "_zeroShotNote": "ZERO-SHOT: the BASE Higgs v3 weights (bosonai/higgs-audio-v3-tts-4b, the HF-cache snapshot the launcher falls back to when HIGGS_MODEL_DIR is unset - the same server a `default` voice runs on) conditioned by ONE reference clip in every request. Asked for by Owen 2026-09-06 ('give me a zero shot option on the higgs/narration modal'). THE CLIP lives PERMANENTLY in the models area - <userData>/runtime/higgs-models/refs/<path> on every machine (Windows: %APPDATA%\\BookForge\\runtime\\higgs-models\\refs; Mac: ~/Library/Application Support/BookForge/runtime/higgs-models/refs), beside runtime/higgs-models/base and next to runtime/orpheus-models; a bare file name here resolves against that folder (higgsReferenceClipPath) and is refused by name if the file is not there. Owen, 2026-09-06: 'ref clips can be saved permanently in the same area where models are saved.' NOT in the repo - it is a voice artifact staged per machine like a checkpoint. Chosen by the training session from its TREATED corpus (denoised, bed-normalized: the clone copies whatever noise the clip carries), one clean ~15 s clip rather than two joined; its transcript is the corpus row's book-exact text (no digits, no quotes). Training's length guidance: 12-16 s (HIGH that it works - a 13.2 s clip drove a whole 37-chunk subset on SGLang; MEDIUM that it is optimal, length not swept). COST: ~25 placeholder positions per second of reference plus the transcript, charged to SGLang-Omni's hard 4096 context by narrator's sgl_served.reference_token_bound; at 600 chars the frame cap keeps its full 2.0x ceiling. HOW IT RIDES: on SGLang-Omni as `references: [{data: <base64 wav>, media_type, text}]` - the SpeechReference `data` branch, which never touches the server's filesystem (no --allowed-local-media-path); on vllm-omni as one data: URI; on the Mac's MLX arm encoded once at load. SAMPLING on the served arm is narrator's SERVER_DEFAULT_SAMPLING (T 1.0 / top_p 0.95 / top_k 50 - the numbers every training run used; the base snapshot ships NO generation_config.json, so there is nothing to read); the `sampling` block below is a MIRROR of that for the reader, not a source. Training's LOW-confidence preference for T 0.8 on base is recorded here and not applied - unmeasured. EXPECT the fine-tunes' ~1-in-10 retake rate or worse (run-ons and early stops are stochastic per render); chunk-to-chunk identity drift is UNMEASURED for zero-shot. Identity of a clone against the narrator: ~0.68 cosine one clip vs 0.766 self-ceiling (see _referenceComment), i.e. 92 % of the fine-tune's identity and none of its phrasing.",
      "_relationNote": "a DIFFERENT PRODUCT from the `mistborn` fine-tune (see the deathstalker zero-shot entry for the measurement); base weights + this clip",
      "backends": {
        "served": {
          "maxChars": 600,
          "maxCharsSource": "placeholder",
          "targetChars": 600,
          "targetCharsSource": "the zero-shot wall (Owen 2026-09-06, 'get it working'): <= 600 chars keeps its text, 900 drops the tail reproducibly, cloned or not - HIGH on vllm-omni where it was measured, MEDIUM on SGLang (training has not re-measured base + clip there). See the `default` entry's _maxCharsNote for the measurement.",
          "edgeFadeMs": {
            "in": 10,
            "out": 25
          },
          "referenceSecondsCap": 30,
          "allowedControls": []
        },
        "mlx": {
          "maxChars": 600,
          "maxCharsSource": "placeholder",
          "targetChars": 600,
          "targetCharsSource": "the zero-shot placeholder, UNMEASURED on the MLX arm for this clip (the same placeholder the `default` entry carries there)",
          "edgeFadeMs": {
            "in": 10,
            "out": 25
          },
          "referenceSecondsCap": 30,
          "allowedControls": []
        }
      }
    }
  ],
  "_checkpointShapeComment": "A CHECKPOINT VOICE NAMES ITS MERGED DIRECTORY ONCE PER ARM: `voice.checkpoint: { \"wsl\": \"<absolute guest path>\", \"darwin\": \"<path relative to the app's userData>\" }`. The single `voice.checkpointDir` string it replaced (retired 2026-09-05) is REFUSED by name if it reappears — one string can only be one machine's path, and it was the WSL guest's, so a Mac render was handed a /home/telltale/... directory that does not exist there and died inside narrator. The keys are the FILESYSTEM the weights sit on, which is why they are not the doctor's HiggsArm vocabulary ('wsl' | 'mlx' | 'none'): that answers which BACKEND was examined, and its third member is a machine no checkpoint can be staged on at all. The two are 1:1 today (wsl<->wsl, darwin<->mlx) and electron/higgs-models.ts is the one place that says so. A MISSING ARM MEANS NOT LOADABLE THERE — never the other arm's path, never a search of the disk: `listRenderableHiggsModels()` and `resolveHiggsModel()` are arm-aware, so the narration dropdown and Listen show a checkpoint voice as loadable only on a machine the catalog stages it for, and higgsVoicesDocument() writes the ONE path for the arm being spawned.",
  "_pathComment": "PATHS IN THIS FILE ARE HOST-NATIVE, and they are translated for the guest at DOCUMENT-WRITE time, per spawn arm (see higgsVoicesDocument). An earlier draft stored WSL-native paths and handed them to every arm untranslated, which is correct on the Windows+WSL arm by accident and meaningless on macOS/Linux, where there is no guest to be native to. `toGuestPath` translates a Windows drive path (C:... -> /mnt/c/...) and a \\wsl$ UNC (the form tool-paths.ts already documents for orpheusModelsDir) and passes an already-guest-form path through unchanged, so a catalog written on any of the three machines resolves on all of them."
}
