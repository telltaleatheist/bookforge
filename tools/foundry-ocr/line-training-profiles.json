{
  "_comment": [
    "ocr LINE-model training profiles. Merge into the rig's training_profiles.json",
    "(/mnt/c/Users/tellt/Projects/orpheus-finetune/training_profiles.json) before a run — that file",
    "is not version controlled, so THIS is the reviewable source of truth. tools/foundry-ocr/train-line.sh",
    "does the merge, and backs the rig's copy up first.",
    "",
    "These are NOT tools/foundry-ocr/training-profiles.json. That file holds ocr_v1_4b / ocr_v1_06b,",
    "which train the BLOCK model: input = one OCR block, target = an edit list under the",
    "tools/foundry-ocr/edits.mjs contract. These profiles train the LINE model: input = one band's",
    "Tesseract --psm 7 output, target = the corrected line. Different unit, different corpus",
    "(sft-line/, not sft/), different output contract. Do not cross them.",
    "",
    "DERIVED FROM dagger_v1, deliberately, because dagger is the closest cousin that actually",
    "trained and shipped: same base family, same tiny unit of text, same near-verbatim copying,",
    "same chat_jsonl + assistant_only_loss harness. Everything below that differs from dagger_v1",
    "is listed in the description of the profile that changes it, with why.",
    "",
    "THREE THINGS ARE NOT NEGOTIABLE:",
    "  1. template_kwargs.enable_thinking = false. Qwen3's template with thinking disabled inserts",
    "     an empty <think>\\n\\n</think> block. Train without it and every served prompt is a shape",
    "     the model never saw. blocks was bitten by exactly this; dagger_v1 sets it; so do these.",
    "  2. load_in_4bit = false. This is character-level copying and quantization costs character",
    "     fidelity. A 0.6B in bf16 is ~1.2GB; there is nothing worth saving.",
    "  3. JUDGE BY tools/foundry-ocr/eval-line.py, NEVER BY LOSS. Read `degraded` first, every time.",
    "     A model that changes nothing and a model that rewrites the author produce the same loss",
    "     curve and opposite books."
  ],
  "profiles": {
    "ocr_line_v1_1_06b": {
      "kind": "text_sft",
      "description": "ocr v1.1 LINE corrector — ocr_line_v1_06b's recipe EXACTLY, retrained on a re-minted corpus. NOT ONE HYPERPARAMETER DIFFERS from ocr_line_v1_06b: same base, same LoRA, same schedule, same seed 3407, same max_seq_length 512 (re-measured on the new corpus: 47,270 rows give p50 163, p95 181, p99 201, max 246 — identical to v1, zero rows over 512). It exists ONLY so the rebuilt corpus gets its own run id and therefore its own output dir, {run_name}_lora: v1 is being scored and /home/telltale/xtts_ft/ocr_line_v1_06b_lora must survive. WHAT CHANGED IS THE DATA. tools/ocr-lab/align-epub.py commits 9a1666f + 9b417eb re-minted wrap-hyphenated words as a SPLIT: the hyphen line's truth keeps the printed fragment ('ad-') and the continuation line's truth keeps its half ('mit it, ...'). Before 9b417eb the continuation's word-tail was silently DELETED from the truth, so ~17k rows per corpus taught the model to delete a word the scanner had read correctly, and the builder's edge-token rung was papering over it. Measured at the raw mint over the 10 aligned books: the continuation line's truth first word disagreed with the OCR's on 83.5% of hyphen rows before and 7.4% after; the hyphen line's last word, 84.3% before and 6.2% after. In the built corpus the tail-deletion detector falls 126 -> 9 and B-row fragment preservation rises 90.8% -> 94.1%. Downstream in build-dataset.py the hyphen-repair rung is now a no-op on 23,371 of 24,089 rows it fires on (it re-substitutes a fragment the truth already carries), hyphen rows dropped for lack of repair evidence collapse 5,638 -> 932, and whole-edge-word restorations collapse 16,268 -> 3,645 because the aligner no longer produces the artifact they were patching. Corpus: 47,270 train / 21,696 eval / 3,586 eval-german, same holdouts (deathstalker-coda + michelle-remembers), same 50.0% train identity share. The 4 born-digital books aligned by align-pdftext.py (rise-and-fall, gods-people, understanding-jehovahs-witnesses, was-hitler-an-atheist) were deliberately NOT re-minted: that aligner reads truth off PDF text-layer geometry per printed line, so it already emitted hyphens as splits (>99.8%) and re-running align-epub.py on them would swap their truth source, not fix them. STAGE THIS RUN TO ITS OWN REMOTE_DIR (REMOTE_DIR=$HOME/ocr-line-v1_1): the default $HOME/ocr-line holds the corpus v1 was launched against.",
      "model": {
        "id": "Qwen/Qwen3-0.6B",
        "max_seq_length": 512,
        "load_in_4bit": false
      },
      "lora": {
        "r": 32,
        "alpha": 32,
        "dropout": 0.0,
        "target_modules": [
          "q_proj",
          "k_proj",
          "v_proj",
          "o_proj",
          "gate_proj",
          "up_proj",
          "down_proj"
        ]
      },
      "training": {
        "learning_rate": 0.0001,
        "optimizer": "adamw_8bit",
        "per_device_train_batch_size": 4,
        "gradient_accumulation_steps": 4,
        "max_epochs": 3,
        "early_stopping_patience": 2,
        "save_total_limit": 4,
        "warmup_ratio": 0.05,
        "weight_decay": 0.01,
        "lr_scheduler_type": "cosine",
        "seed": 3407
      },
      "output": {
        "lora_dir_template": "{run_name}_lora",
        "merged_dir_template": "{run_name}_merged"
      },
      "dataset": {
        "format": "chat_jsonl",
        "messages_field": "messages",
        "assistant_only_loss": true,
        "template_kwargs": {
          "enable_thinking": false
        }
      }
    },
    "ocr_line_v1_1_4b": {
      "kind": "text_sft",
      "description": "ocr v1.1 LINE corrector at 4B - ocr_line_v1_4b's recipe EXACTLY (Qwen3-4B, batch 2 x accum 8, seed 3407), trained on the re-minted v1.1 corpus (see ocr_line_v1_1_06b for the full corpus story: wrap-hyphens minted as splits, tail-deletions 126 -> 9, hyphen no-evidence drops 5,638 -> 932). Promoted from size control to the v1.1 primary by measurement, Aug 1 2026: scoring ocr_line_v1_06b showed the thin-prior failure this profile's description predicted - unguarded 0.6B is a net NEGATIVE on English headline CER (0.476% vs 0.431% do-nothing), and on the German canary it substitutes rare words with frequent ones at tiny edit distance (Danemark -> Deutschland, Rustung -> Ruckschritt), a CER-invisible factual falsehood that would be narrated. Owner's call: capacity and data over guard machinery. Run id gives its own output dir {run_name}_lora; stage to REMOTE_DIR=$HOME/ocr-line-v1_1 (the default $HOME/ocr-line holds v1's corpus).",
      "model": {
        "id": "Qwen/Qwen3-4B",
        "max_seq_length": 512,
        "load_in_4bit": false
      },
      "lora": {
        "r": 32,
        "alpha": 32,
        "dropout": 0.0,
        "target_modules": [
          "q_proj",
          "k_proj",
          "v_proj",
          "o_proj",
          "gate_proj",
          "up_proj",
          "down_proj"
        ]
      },
      "training": {
        "learning_rate": 0.0001,
        "optimizer": "adamw_8bit",
        "per_device_train_batch_size": 2,
        "gradient_accumulation_steps": 8,
        "max_epochs": 3,
        "early_stopping_patience": 2,
        "save_total_limit": 4,
        "warmup_ratio": 0.05,
        "weight_decay": 0.01,
        "lr_scheduler_type": "cosine",
        "seed": 3407
      },
      "output": {
        "lora_dir_template": "{run_name}_lora",
        "merged_dir_template": "{run_name}_merged"
      },
      "dataset": {
        "format": "chat_jsonl",
        "messages_field": "messages",
        "assistant_only_loss": true,
        "template_kwargs": {
          "enable_thinking": false
        }
      }
    },
    "ocr_sent_v1_4b": {
      "kind": "text_sft",
      "description": "ocr SENTENCE corrector v1, Qwen3-4B — the retrain the 2026-08-05 guard measurement asked for. The shipped line model served sentence units repairs LESS and false-edits 12-25% (vs 0.6-7% on lines): a distribution mismatch, and this profile closes it by training on the unit serving actually produces. Corpus: rubric/galley/sft-sent (bookforge 5afef2e) — 31,600 train / 4,979 eval / 770 eval-german sentence units packed from the SAME raw line pairs, holdouts and repair ladder as the line corpus, 50.0% train identity at UNIT level, units p50 331 chars at 4.66 lines each, character-exact eval parity with sft-line asserted by the build. The units were cut by the mirror of foundry src/ocr/sentences.ts (fa4c221: send-more-through boundaries — abbreviation guard incl. German, footnote-reference digits, decimal guard), and serving judges per-run (foundry ab8d303), so training unit == serving unit is a build invariant, not a hope. RECIPE IS ocr_line_v1_1_4b UNCHANGED except max_seq_length 512 -> 768, MEASURED with the real Qwen3 tokenizer, enable_thinking=false: train p50 283 / p90 319 / p99 405 / max 573, eval-german max 579. 23 rows exceed 512 (the tail is German at ~2.2 chars/token — an average-case estimate says 512 holds and is wrong); ZERO exceed 768, and text_sft refuses to truncate, so 512 kills the run partway. STAGE TO REMOTE_DIR=$HOME/ocr-sent-v1. Judge by tools/foundry-ocr/eval-guard.py under per-run against ocr_line_v1_1_4b on the same holdout books, degraded first, do-nothing baseline printed alongside — never by loss. Identity-share sweep variants (0.65/0.8) re-derive from sft-sent/units.jsonl without remining.",
      "model": {
        "id": "Qwen/Qwen3-4B",
        "max_seq_length": 768,
        "load_in_4bit": false
      },
      "lora": {
        "r": 32,
        "alpha": 32,
        "dropout": 0.0,
        "target_modules": [
          "q_proj",
          "k_proj",
          "v_proj",
          "o_proj",
          "gate_proj",
          "up_proj",
          "down_proj"
        ]
      },
      "training": {
        "learning_rate": 0.0001,
        "optimizer": "adamw_8bit",
        "per_device_train_batch_size": 2,
        "gradient_accumulation_steps": 8,
        "max_epochs": 3,
        "early_stopping_patience": 2,
        "save_total_limit": 4,
        "warmup_ratio": 0.05,
        "weight_decay": 0.01,
        "lr_scheduler_type": "cosine",
        "seed": 3407
      },
      "output": {
        "lora_dir_template": "{run_name}_lora",
        "merged_dir_template": "{run_name}_merged"
      },
      "dataset": {
        "format": "chat_jsonl",
        "messages_field": "messages",
        "assistant_only_loss": true,
        "template_kwargs": {
          "enable_thinking": false
        }
      }
    },
    "ocr_sent_v1_4b_safe": {
      "kind": "text_sft",
      "description": "FALLBACK for ocr_sent_v1_4b: identical except batch 1 x accum 16, the shape every profile on this box has survived. Same effective batch of 16 so the recipe stays comparable. Exists for the same reason the other _safe profiles do, with the multiplier stated: the OOM ceiling is the lm_head logits tensor (batch x seq x 151936 vocab), and 768 is 1.5x the sequence width the 4B line profile proved at batch 2 — an untested width on a box that measured 11 GB free with a job in it.",
      "model": {
        "id": "Qwen/Qwen3-4B",
        "max_seq_length": 768,
        "load_in_4bit": false
      },
      "lora": {
        "r": 32,
        "alpha": 32,
        "dropout": 0.0,
        "target_modules": [
          "q_proj",
          "k_proj",
          "v_proj",
          "o_proj",
          "gate_proj",
          "up_proj",
          "down_proj"
        ]
      },
      "training": {
        "learning_rate": 0.0001,
        "optimizer": "adamw_8bit",
        "per_device_train_batch_size": 1,
        "gradient_accumulation_steps": 16,
        "max_epochs": 3,
        "early_stopping_patience": 2,
        "save_total_limit": 4,
        "warmup_ratio": 0.05,
        "weight_decay": 0.01,
        "lr_scheduler_type": "cosine",
        "seed": 3407
      },
      "output": {
        "lora_dir_template": "{run_name}_lora",
        "merged_dir_template": "{run_name}_merged"
      },
      "dataset": {
        "format": "chat_jsonl",
        "messages_field": "messages",
        "assistant_only_loss": true,
        "template_kwargs": {
          "enable_thinking": false
        }
      }
    },
    "ocr_line_v1_06b": {
      "kind": "text_sft",
      "description": "ocr v1 LINE corrector, Qwen3-0.6B, 16-bit LoRA. Input = one OCR'd line from the band pipeline (render 200dpi -> projection-profile bands -> one tesseract --psm 7 per line); output = the corrected line, or the line unchanged. Sits between recognition and block grouping in docs/OCR_LAB.md's pipeline, which is why the unit is a line and not a block: at this point in the pipeline blocks do not exist yet. Corpus: tools/foundry-ocr/build-dataset.py over <lab>/<book>/scores/epub-align-pairs.json, held out on deathstalker-coda (fiction) + michelle-remembers (nonfiction), train held at 50% identity rows because restraint is the whole game. The holdouts are chosen for what they MEASURE: michelle-remembers is tier-1 publisher truth over a bad IA scan, so its error density is high and informative, and it is the only large book the band pipeline was never tuned against — deathstalker 1 was the calibration book and therefore has to stay in TRAIN, or the score measures the pipeline's own tuning instead of the model. THE MODEL CANNOT SEE THE NEXT LINE, so it is trained to leave a wrap hyphen alone and never to extend the first or last word of a line; the dataset builder enforces both. CHANGED FROM dagger_v1: max_epochs 4 -> 3. dagger took 4 because its corpus was 2,598 rows with one book carrying 98% of the positives; this corpus is ~25k rows across 7+ books, and BLOCKS_TRAINING.md section 5 records that three runs out of three peaked at epoch 1. early_stopping_patience 2 and best-checkpoint selection make 3 a ceiling, not a target. max_seq_length 512 is MEASURED, not assumed: 46,726 train rows through the Qwen3 tokenizer with enable_thinking=false give p50 163, p95 181, p99 201, max 246. Zero rows exceed 512. This matters because text_sft refuses to truncate -- one unmeasured long row kills the run partway through.",
      "model": {
        "id": "Qwen/Qwen3-0.6B",
        "max_seq_length": 512,
        "load_in_4bit": false
      },
      "lora": {
        "r": 32,
        "alpha": 32,
        "dropout": 0.0,
        "target_modules": [
          "q_proj",
          "k_proj",
          "v_proj",
          "o_proj",
          "gate_proj",
          "up_proj",
          "down_proj"
        ]
      },
      "training": {
        "learning_rate": 0.0001,
        "optimizer": "adamw_8bit",
        "per_device_train_batch_size": 4,
        "gradient_accumulation_steps": 4,
        "max_epochs": 3,
        "early_stopping_patience": 2,
        "save_total_limit": 4,
        "warmup_ratio": 0.05,
        "weight_decay": 0.01,
        "lr_scheduler_type": "cosine",
        "seed": 3407
      },
      "output": {
        "lora_dir_template": "{run_name}_lora",
        "merged_dir_template": "{run_name}_merged"
      },
      "dataset": {
        "format": "chat_jsonl",
        "messages_field": "messages",
        "assistant_only_loss": true,
        "template_kwargs": {
          "enable_thinking": false
        }
      }
    },
    "ocr_line_v1_06b_safe": {
      "kind": "text_sft",
      "description": "FALLBACK for ocr_line_v1_06b: identical except batch 1 x accum 16, the shape every profile on this box has survived. Same effective batch of 16 so the recipe stays comparable; step count and wall clock are not. Exists for the same reason dagger_v1_safe does — the OOM ceiling on a small model is the lm_head logits tensor (batch x seq x 151936 vocab), which does not shrink with the model, so an untested batch width is the one thing that can lose a night.",
      "model": {
        "id": "Qwen/Qwen3-0.6B",
        "max_seq_length": 512,
        "load_in_4bit": false
      },
      "lora": {
        "r": 32,
        "alpha": 32,
        "dropout": 0.0,
        "target_modules": [
          "q_proj",
          "k_proj",
          "v_proj",
          "o_proj",
          "gate_proj",
          "up_proj",
          "down_proj"
        ]
      },
      "training": {
        "learning_rate": 0.0001,
        "optimizer": "adamw_8bit",
        "per_device_train_batch_size": 1,
        "gradient_accumulation_steps": 16,
        "max_epochs": 3,
        "early_stopping_patience": 2,
        "save_total_limit": 4,
        "warmup_ratio": 0.05,
        "weight_decay": 0.01,
        "lr_scheduler_type": "cosine",
        "seed": 3407
      },
      "output": {
        "lora_dir_template": "{run_name}_lora",
        "merged_dir_template": "{run_name}_merged"
      },
      "dataset": {
        "format": "chat_jsonl",
        "messages_field": "messages",
        "assistant_only_loss": true,
        "template_kwargs": {
          "enable_thinking": false
        }
      }
    },
    "ocr_line_v1_4b": {
      "kind": "text_sft",
      "description": "SIZE CONTROL for ocr_line_v1_06b, and the owner should expect to need it. Identical corpus and recipe, Qwen3-4B. The block model's profile argues 4B is required because the job is LEXICAL rather than positional, and this corpus makes that concrete: himmler-a-life is now the largest book in TRAIN and its real errors are 'Reichsftihrer' -> 'Reichsfuehrer/Reichsführer', 'Fugoslawien' -> 'Jugoslawien', 'Raterepublik' -> 'Räterepublik' — German proper nouns and umlaut damage, roughly 7,000 edit rows of it. A thin language prior does not merely fail to fix those, it 'corrects' them toward something more English-looking, and the result is narrated. NOTE the holdout change moved the German evidence from eval into train, so the 0.6B's handling of it is no longer measured by the headline: score BOTH sizes and read `degraded` on the German-bearing rows specifically, or the risk goes unobserved. 0.6B is primary only because the pipeline runs this model per LINE, tens of thousands of times per book, where a 4B's inference cost is a real number. batch 2 x accum 8 to match the 4B block profile.",
      "model": {
        "id": "Qwen/Qwen3-4B",
        "max_seq_length": 512,
        "load_in_4bit": false
      },
      "lora": {
        "r": 32,
        "alpha": 32,
        "dropout": 0.0,
        "target_modules": [
          "q_proj",
          "k_proj",
          "v_proj",
          "o_proj",
          "gate_proj",
          "up_proj",
          "down_proj"
        ]
      },
      "training": {
        "learning_rate": 0.0001,
        "optimizer": "adamw_8bit",
        "per_device_train_batch_size": 2,
        "gradient_accumulation_steps": 8,
        "max_epochs": 3,
        "early_stopping_patience": 2,
        "save_total_limit": 4,
        "warmup_ratio": 0.05,
        "weight_decay": 0.01,
        "lr_scheduler_type": "cosine",
        "seed": 3407
      },
      "output": {
        "lora_dir_template": "{run_name}_lora",
        "merged_dir_template": "{run_name}_merged"
      },
      "dataset": {
        "format": "chat_jsonl",
        "messages_field": "messages",
        "assistant_only_loss": true,
        "template_kwargs": {
          "enable_thinking": false
        }
      }
    },
    "ocr_sent_v1_1_4b": {
      "kind": "text_sft",
      "description": "ocr SENTENCE corrector v1.1, Qwen3-4B. Corpus: rubric/galley/sft-sent-v1_1 — 43,886 train / 4,979 eval / 770 eval-german. TRAIN ONLY differs from sft-sent; eval and eval-german are asserted unit-for-unit IDENTICAL, so this is directly comparable with ocr_sent_v1_4b's scores. Two changes, both answering what results-sent-v1 measured. (1) TYPOGRAPHY: the v1 corpus contained zero curly quotes, em dashes or ellipses on either side — align-epub.py folded them to ASCII before a pair was written — and 13 of the 15 edits ocr_sent_v1_4b makes on a real Kershaw EPUB are that fold played back. 63,623 already-correct train lines get the marks Tesseract actually read; 62.8% of train units now carry one. (2) GOLD-JOIN: gold said `ad-mit` wherever a word wrapped; 17,334 wraps are now closed in TRAIN gold where the book's own vocabulary says the fragments are one word (954 kept as real compounds, 3,126 as ambiguous). ⚠️ THE JOIN CONTRADICTS THIS PROFILE'S SYSTEM PROMPT, which still says 'keep a hyphen at the end of the line exactly as it is' — that is ocr_sent_v1_1b_4b's question. Train identity is 44.8%, 4,526 short of 50%, because the join converted 9,115 identity units into edit units. STAGE TO REMOTE_DIR=$HOME/ocr-sent-v1_1. train max 615 tokens. RECIPE IS ocr_sent_v1_4b UNCHANGED — same base, same LoRA, same schedule, max_seq_length 768. Token lengths RE-MEASURED with the real Qwen3 tokenizer (enable_thinking=false) on the corpus this profile names; 0 rows exceed 768. Judge by tools/foundry-ocr/eval-guard.py under per-run against ocr_sent_v1_4b, degraded first, do-nothing baseline alongside — never by loss.",
      "model": {
        "id": "Qwen/Qwen3-4B",
        "max_seq_length": 768,
        "load_in_4bit": false
      },
      "lora": {
        "r": 32,
        "alpha": 32,
        "dropout": 0.0,
        "target_modules": [
          "q_proj",
          "k_proj",
          "v_proj",
          "o_proj",
          "gate_proj",
          "up_proj",
          "down_proj"
        ]
      },
      "training": {
        "learning_rate": 0.0001,
        "optimizer": "adamw_8bit",
        "per_device_train_batch_size": 2,
        "gradient_accumulation_steps": 8,
        "max_epochs": 3,
        "early_stopping_patience": 2,
        "save_total_limit": 4,
        "warmup_ratio": 0.05,
        "weight_decay": 0.01,
        "lr_scheduler_type": "cosine",
        "seed": 3407
      },
      "output": {
        "lora_dir_template": "{run_name}_lora",
        "merged_dir_template": "{run_name}_merged"
      },
      "dataset": {
        "format": "chat_jsonl",
        "messages_field": "messages",
        "assistant_only_loss": true,
        "template_kwargs": {
          "enable_thinking": false
        }
      }
    },
    "ocr_sent_v1_1b_4b": {
      "kind": "text_sft",
      "description": "ocr SENTENCE corrector v1.1 under PROMPT VARIANT B, Qwen3-4B. Corpus: rubric/galley/sft-sent-v1_1-promptb — byte-identical to sft-sent-v1_1 except the system prompt, which is SYSTEM_B: 'a passage of text' rather than 'a single line', a typography-preservation rule, and the trailing-hyphen rule split so an interior wrap is closed and only the passage's LAST hyphen is kept. It is a DRAFT — foundry src/ocr/prompt.ts is untouched and nothing serves this text, so a win here is an argument for changing prompt.ts, not permission to. Run it ONLY as the paired B of ocr_sent_v1_1_4b: same corpus, same seed, one variable. STAGE TO REMOTE_DIR=$HOME/ocr-sent-v1_1b. train max 683 tokens, 113 rows over 512. RECIPE IS ocr_sent_v1_4b UNCHANGED — same base, same LoRA, same schedule, max_seq_length 768. Token lengths RE-MEASURED with the real Qwen3 tokenizer (enable_thinking=false) on the corpus this profile names; 0 rows exceed 768. Judge by tools/foundry-ocr/eval-guard.py under per-run against ocr_sent_v1_4b, degraded first, do-nothing baseline alongside — never by loss.",
      "model": {
        "id": "Qwen/Qwen3-4B",
        "max_seq_length": 768,
        "load_in_4bit": false
      },
      "lora": {
        "r": 32,
        "alpha": 32,
        "dropout": 0.0,
        "target_modules": [
          "q_proj",
          "k_proj",
          "v_proj",
          "o_proj",
          "gate_proj",
          "up_proj",
          "down_proj"
        ]
      },
      "training": {
        "learning_rate": 0.0001,
        "optimizer": "adamw_8bit",
        "per_device_train_batch_size": 2,
        "gradient_accumulation_steps": 8,
        "max_epochs": 3,
        "early_stopping_patience": 2,
        "save_total_limit": 4,
        "warmup_ratio": 0.05,
        "weight_decay": 0.01,
        "lr_scheduler_type": "cosine",
        "seed": 3407
      },
      "output": {
        "lora_dir_template": "{run_name}_lora",
        "merged_dir_template": "{run_name}_merged"
      },
      "dataset": {
        "format": "chat_jsonl",
        "messages_field": "messages",
        "assistant_only_loss": true,
        "template_kwargs": {
          "enable_thinking": false
        }
      }
    },
    "ocr_sent_v1_2_4b": {
      "kind": "text_sft",
      "description": "ocr SENTENCE corrector v1.2, Qwen3-4B — v1.1's fix moved UPSTREAM. Corpus: rubric/galley/sft-sent-v1_2 — 39,510 train / 4,983 eval / 772 eval-german, built from the v2 alignment (epub-align-pairs-v2.json), where align-epub.py and align-pdftext.py emit the book's own characters and fold only for MATCHING. The alignment is provably unmoved: 311,207 of 311,207 pairs, delta 0 on every book, same sim, same cer. So EDIT targets carry typography too, not just the identity rows v1.1 could reach — 14,534 of 19,755 edit units. --ocr-typography keep is what stops the new rows teaching normalisation: 44,581 rows where the two sides differ only in how a mark is SET keep the scanner's, because half the lab's EPUBs are themselves OCR-derived and set every quote straight. Train identity is EXACTLY 50.0% (--identity-share-mode balance dropped 4,338 edit units to get there). ⚠️ ITS EVAL IS A NEW EVAL. sft-sent-v1_2/eval.jsonl covers the same pages of the same two holdout books with the characters the books print, and is 643 + -1 non-whitespace characters off sft-sent/eval.jsonl. Scoring this model on the OLD eval measures the old character distribution; scoring ocr_sent_v1_4b on the NEW one asks it for marks it was never shown. Report both, and say which. STAGE TO REMOTE_DIR=$HOME/ocr-sent-v1_2. train max 615 tokens. RECIPE IS ocr_sent_v1_4b UNCHANGED — same base, same LoRA, same schedule, max_seq_length 768. Token lengths RE-MEASURED with the real Qwen3 tokenizer (enable_thinking=false) on the corpus this profile names; 0 rows exceed 768. Judge by tools/foundry-ocr/eval-guard.py under per-run against ocr_sent_v1_4b, degraded first, do-nothing baseline alongside — never by loss.",
      "model": {
        "id": "Qwen/Qwen3-4B",
        "max_seq_length": 768,
        "load_in_4bit": false
      },
      "lora": {
        "r": 32,
        "alpha": 32,
        "dropout": 0.0,
        "target_modules": [
          "q_proj",
          "k_proj",
          "v_proj",
          "o_proj",
          "gate_proj",
          "up_proj",
          "down_proj"
        ]
      },
      "training": {
        "learning_rate": 0.0001,
        "optimizer": "adamw_8bit",
        "per_device_train_batch_size": 2,
        "gradient_accumulation_steps": 8,
        "max_epochs": 3,
        "early_stopping_patience": 2,
        "save_total_limit": 4,
        "warmup_ratio": 0.05,
        "weight_decay": 0.01,
        "lr_scheduler_type": "cosine",
        "seed": 3407
      },
      "output": {
        "lora_dir_template": "{run_name}_lora",
        "merged_dir_template": "{run_name}_merged"
      },
      "dataset": {
        "format": "chat_jsonl",
        "messages_field": "messages",
        "assistant_only_loss": true,
        "template_kwargs": {
          "enable_thinking": false
        }
      }
    },
    "ocr_sent_v1_2b_4b": {
      "kind": "text_sft",
      "description": "ocr SENTENCE corrector v1.2 under PROMPT VARIANT B, Qwen3-4B. Corpus: rubric/galley/sft-sent-v1_2-promptb — sft-sent-v1_2 with SYSTEM_B, a DRAFT sentence-unit prompt whose typography rule now matches what the corpus actually carries on both sides. The paired B of ocr_sent_v1_2_4b; one variable. STAGE TO REMOTE_DIR=$HOME/ocr-sent-v1_2b. train max 683 tokens, 113 rows over 512. RECIPE IS ocr_sent_v1_4b UNCHANGED — same base, same LoRA, same schedule, max_seq_length 768. Token lengths RE-MEASURED with the real Qwen3 tokenizer (enable_thinking=false) on the corpus this profile names; 0 rows exceed 768. Judge by tools/foundry-ocr/eval-guard.py under per-run against ocr_sent_v1_4b, degraded first, do-nothing baseline alongside — never by loss.",
      "model": {
        "id": "Qwen/Qwen3-4B",
        "max_seq_length": 768,
        "load_in_4bit": false
      },
      "lora": {
        "r": 32,
        "alpha": 32,
        "dropout": 0.0,
        "target_modules": [
          "q_proj",
          "k_proj",
          "v_proj",
          "o_proj",
          "gate_proj",
          "up_proj",
          "down_proj"
        ]
      },
      "training": {
        "learning_rate": 0.0001,
        "optimizer": "adamw_8bit",
        "per_device_train_batch_size": 2,
        "gradient_accumulation_steps": 8,
        "max_epochs": 3,
        "early_stopping_patience": 2,
        "save_total_limit": 4,
        "warmup_ratio": 0.05,
        "weight_decay": 0.01,
        "lr_scheduler_type": "cosine",
        "seed": 3407
      },
      "output": {
        "lora_dir_template": "{run_name}_lora",
        "merged_dir_template": "{run_name}_merged"
      },
      "dataset": {
        "format": "chat_jsonl",
        "messages_field": "messages",
        "assistant_only_loss": true,
        "template_kwargs": {
          "enable_thinking": false
        }
      }
    },
    "ocr_sent_v1_4_4b": {
      "kind": "text_sft",
      "description": "ocr SENTENCE corrector v1.4, Qwen3-4B — THE JOIN IS TAUGHT AND MEASURED. Corpus: rubric/galley/sft-sent-v1_4-promptb — 43,848 train / 4,983 eval / 772 eval-german, built from the v2 alignment with --gold-join wrap-hyphen --gold-join-scope train+eval --identity-share-mode fill --ocr-typography keep --prompt-variant b. TWO THINGS DIFFER FROM v1.2. (1) --gold-join-scope train+eval: every earlier corpus applied the wrap-hyphen join to TRAIN ONLY and merely REPORTED what it withheld from eval (evalUnitsNotJoined 1532). That charged a degradation for every weld the model performed CORRECTLY on 27% of eval units, which made the capability unmeasurable in the run that exists to acquire it. 1,816 joins are now applied to the eval gold too. Owen's ruling, Aug 5 2026: the model owns hyphen joining; it is not to happen deterministically. (2) --identity-share-mode fill instead of balance: balance discarded 4,338 EDIT units to hold a 50% ratio, and recall — not false edits — is this model's weak axis (1.1% falseEdit against only a third of error mass repaired). Identity lands at 45.1% and every edit unit is kept, giving 24,093 edit units of which 14,958 are genuine letter repair and 9,135 teach the join. PROMPT VARIANT B IS REQUIRED HERE, not optional: variant A says 'keep a hyphen at the end of the line exactly as it is', which directly contradicts 17,229 gold joins. Shipping this model means moving foundry src/ocr/prompt.ts, sentences.py and both crosschecks in ONE commit with it. STAGE TO REMOTE_DIR=$HOME/ocr-sent-v1_4. RECIPE IS ocr_sent_v1_4b UNCHANGED — same base, same LoRA, same schedule, max_seq_length 768. RE-MEASURE token lengths with train-line.sh --preflight; the builder's bound for this corpus is under the 683 already recorded for variant B. Judge by tools/foundry-ocr/eval-guard.py under per-run against ocr_sent_v1_4b, degraded first, do-nothing baseline alongside — never by loss. Score on BOTH evals and say which a number is from.",
      "model": {
        "id": "Qwen/Qwen3-4B",
        "max_seq_length": 768,
        "load_in_4bit": false
      },
      "lora": {
        "r": 32,
        "alpha": 32,
        "dropout": 0.0,
        "target_modules": [
          "q_proj",
          "k_proj",
          "v_proj",
          "o_proj",
          "gate_proj",
          "up_proj",
          "down_proj"
        ]
      },
      "training": {
        "learning_rate": 0.0001,
        "optimizer": "adamw_8bit",
        "per_device_train_batch_size": 2,
        "gradient_accumulation_steps": 8,
        "max_epochs": 3,
        "early_stopping_patience": 2,
        "save_total_limit": 4,
        "warmup_ratio": 0.05,
        "weight_decay": 0.01,
        "lr_scheduler_type": "cosine",
        "seed": 3407
      },
      "output": {
        "lora_dir_template": "{run_name}_lora",
        "merged_dir_template": "{run_name}_merged"
      },
      "dataset": {
        "format": "chat_jsonl",
        "messages_field": "messages",
        "assistant_only_loss": true,
        "template_kwargs": {
          "enable_thinking": false
        }
      }
    }
  }
}
