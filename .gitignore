# See https://docs.github.com/get-started/getting-started-with-git/ignoring-files for more about ignoring files.

# Compiled output
/dist
/tmp
/out-tsc
/bazel-out

# Node
/node_modules
npm-debug.log
yarn-error.log

# IDEs and editors
.idea/
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# Visual Studio Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.history/*

# Python
__pycache__/
*.pyc

# Miscellaneous
/.angular/cache
.sass-cache/
/connect.lock
/coverage
/libpeerconnection.log
testem.log
/typings
__screenshots__/

# Model weights / checkpoints
checkpoints/

# System files
.DS_Store
Thumbs.db
._*
*.dmg
release/*
*.plist
dist/*
.vscode/*
CLAUDE.md
.claude/
.editorconfig
resources/bin/*
summary-*.json
*.ttf

# conda-pack tarballs (multi-GB build artifacts; ymls in packaging/env are committed)
packaging/artifacts/

# staged packaging resources (rebuilt by packaging/stage-resources.js)
resources/e2a/
resources/e2a-env.tar.gz
# ExFAT-orphaned snapshot dirs that couldn't be deleted (renamed aside)
resources/*.stuck-*
resources/.e2a-stuck-*
# persistent seed staging caches (voice/base checkpoints for --seed builds)
packaging/.seed-cache/
packaging/.seed-cache-speakers/

# llama.cpp prebuilt download cache (reused across builds; staged into resources/bin)
.llama-build/

# mupdf download/build scratch (scripts/download-mupdf.js TEMP_DIR; auto-removed
# on success, but left behind — thousands of files — if a build is interrupted)
.mupdf-build/

# ultimate-rvc (XTTS voice-clone experiment) writes its data dir into the cwd it
# runs from; these RVC training/model artifacts are unrelated to BookForge
/models/
/logs/
